---
title: AstrologyWiki GA4 埋点排查报告
date: 2026-07-16
状态: 待彪哥本地验证
关联需求: 2026-07-13-需求清单-astrologywiki.md（Needs 2）
---

# GA4 埋点排查报告

## 背景

用户在无痕窗口 Accept All cookies 后点击 CTA，GA4 实时报告中未出现 `tool_click` 事件，触发本次排查。

---

## 一、GA4 基础状态 ✅

| 项目 | 状态 | 说明 |
|---|---|---|
| Measurement ID | ✅ | `G-G9165W1RZS`，硬编码在主 bundle |
| GA4 脚本加载 | ✅ | 懒加载（首次用户交互后触发），非 `<head>` 静态加载 |
| dataLayer 初始化 | ✅ | `S_()` 创建，`w_()` 管理 |
| Consent Mode | ✅ | `Z0()` 设置 default，`wait_for_update:500` |
| `page_view` 触发 | ✅ | Network 中确认 `collect?v2` 请求 |
| `page_engagement` 触发 | ✅ | Network 中持续确认 |

---

## 二、Consent Mode 工作机制

**初次访问流程（无痕窗口）：**

```
页面加载
→ Z0() 运行：gtag("consent","default",{analytics_storage:"denied", wait_for_update:500})
→ 用户交互（任意操作）→ 懒加载 GA4 脚本（K0()）
→ 用户点击 Accept All → Dc({analytics:true}) → O0("granted") → localStorage 写入 "granted"

若 GA4 脚本在 Accept All 之后才初始化：
→ Z0() 再次读取 localStorage → Nc() 返回 "granted"
→ gtag("consent","default",{analytics_storage:"granted"})  ✅ 直接以 granted 状态启动
```

**结论：** 因为 GA4 是懒加载，用户 Accept All 往往早于 GA4 初始化，实际上 consent 状态是正确的。不存在最初判断的"O0() 缺少 gtag consent update"问题——该问题仅在 GA4 先于 Accept All 初始化时才会触发（概率低）。

---

## 三、事件架构

AstrologyWiki 有两套 CTA 事件，**事件名不同，不可混淆：**

### 3.1 `tool_click` — 文章页 CTA

| 属性 | 值 |
|---|---|
| 触发位置 | Blog 文章页、Wiki 页内嵌 CTA |
| 组件 | `my`（BirthChartCTA） |
| 变体 | nav / sticky / lead / bottom |
| module 映射 | nav→module_a, sticky→module_b, lead→module_c, bottom→article_bottom |
| 参数 | `cta_module`, `cta_variant`, `page_location`, `page_path`, `tool_target` |
| 代码路径 | `onClick:y → we("tool_click", {...})` |

### 3.2 `chart_funnel` — 工具页结果 CTA

| 属性 | 值 |
|---|---|
| 触发位置 | 工具页计算出结果后显示的 "See your full birth chart →" |
| 组件 | `ToolFunnelCTA`（`$`，从 `ToolFunnelCTA-DBP8aSTY.js` 加载） |
| 参数 | `tool`, `sign`, `step`（默认 `full_chart_cta_click`）, `placement`（`tool_result`） |
| 代码路径 | `onClick:u → tv({...}) → we("chart_funnel", sg({...}))` |
| 触发条件 | **仅在用户已输入生日并点击 Calculate 后**，结果区域才渲染此 CTA |

---

## 四、排查结论

### 已确认正常

- GA4 初始化 ✅
- Consent 状态传递 ✅（懒加载机制自然规避了 denied 状态）
- 代码路径连通：`tool_click` 和 `chart_funnel` 均已正确绑定 `onClick`

### 未能在 Network 中直接确认的原因

点击 CTA 后页面立即跳转（SPA 路由切换），GA4 事件发送与导航之间存在时序竞争。Network 中出现的 30-39 个 `collect?v2` 请求，均为跳转后新页面的初始化事件，原页面的 CTA 事件可能已发送但在新页面 Network 记录中不可见。

---

## 五、给彪哥的验证方法

### 方法一：Console 打印（推荐，开发环境）

在 `we()` 函数中临时加一行 `console.log`：

```javascript
we = (o, u = {}) => {
  console.log('[GA4 event]', o, u);  // ← 临时加，确认后删除
  if (!ya() || !I0(o) && !mn()) return;
  w_().push({event: o, ...u});
  window.gtag && Za && window.gtag("event", o, u);
};
```

**预期结果：**
- 点击 Nav "Get Free Birth Chart" → Console 打印 `[GA4 event] tool_click {...}`
- 计算后点击 "See your full birth chart →" → Console 打印 `[GA4 event] chart_funnel {...}`

### 方法二：GA4 后台事件报告（24小时后）

GA4 → 报告 → 事件 → 查找 `tool_click` 和 `chart_funnel`。实时报告有时序限制，事件报告更可靠。

---

## 六、待确认需求

| 需求 | 状态 | 说明 |
|---|---|---|
| Oracle CTA 埋点 | ❌ 未实现 | Oracle 模块在 7/13 需求文档之后上线，`oracle_cta_click` 事件未包含在内，需单独实现 |
| P1 模块 CTA（D/G/H/J/K） | ❌ 未实现 | 文章中段内联 CTA、右侧悬浮工具卡等，仍在 backlog |

---

*文件：inbox/00-inbox/2026-07-16-ga4埋点排查报告.md*
*版本：v1.0 | 2026-07-16*
*关联：inbox/00-inbox/2026-07-13-需求清单-astrologywiki.md*
