---
title: 趋势词桥接逻辑规范
date: 2026-07-06
version: v1.1
owner: Ma Boyang
适用系统: 路径B自动化建站系统 Module 1.5（场景分叉）Path ① 判断
关联文件: inbox/06-tasks/2026-07-02-路径B自动化建站系统-需求文档.md
---

# 趋势词桥接逻辑规范

---

## 一、问题背景

Module 1.5 的 Path ① 判断问题是："这个词有明确的占星/星座角度？"

当前执行时，操作者将"明确的占星角度"理解为**词本身带占星属性**，导致：

- "Bing Hastert"、"Harry Kane"、"World Cup 2026" 等高热度词全部被判为无占星角度
- 系统运行 5 天零产出
- 而这类词实际上已通过 `[人名] birth chart`、`[赛事] astrology` 的形式为 astrologywiki 带来高曝光（GSC 验证）

**根本原因：** 趋势词不需要本身与占星相关，只需要**可以通过固定公式桥接到占星内容**，即可走 Path ①。

---

## 二、完整判断流程

Module 1 发现候选词后，按以下顺序依次判断：

```
Step 1：重复检查（新增）
  → 检查 astrologywiki sitemap，桥接词对应页面是否已存在
  → 已存在 → 转内容更新流程（见第三节），不再走 Path ① 新建
  → 不存在 → 进入 Step 2

Step 2：Path ① 资格判断
  → Level 1：趋势词本身属于占星话题（天象事件、星座概念等）→ 通过
  → Level 2：趋势词属于可桥接类型（见第四节）→ 通过
  → 两级都未通过 → 继续判断 Path ②

Step 3：桥接词 SERP 验证（新增，仅 Level 2 执行）
  → 验证目标：桥接词本身（如 `Harry Kane birth chart`），而非原始趋势词
  → 前3名有超级平台（Wikipedia 人物页、Reddit 等）→ ⚠️ 标注竞争强，人工确认
  → 前10名平均 DR > 70 → ❌ 否决
  → 前10名有小站内页排名 → ✅ 加分，优先推进

Step 4：出生日期验证（热门人物专用，见第五节）

Step 5：Path ① 通过 → 生成输出通知（见第六节）
```

---

## 三、已有页面的处理（重复检查逻辑）

Step 1 发现 astrologywiki 已有对应页面时，不走新建流程，改为：

| 情况 | 处理方式 |
|---|---|
| 页面存在，趋势仍在窗口期 | 推送内容更新通知，内容负责人追加段落 + 更新日期（参考博客优化规范 §四）|
| 页面存在，趋势已过期 | 不处理，忽略本次触发 |
| 页面存在，但该人物/赛事有重大新进展 | 推送更新通知，注明新进展摘要 |

> sitemap 检查方式：`curl -s https://www.astrologywiki.com/sitemap.xml | grep "[桥接词slug]"`

---

## 四、可桥接类型定义

| 类型 | 识别方式 | 桥接公式候选 | 内容产出示例 | 适用路径 | 内容时效 |
|---|---|---|---|---|---|
| **热门人物**（运动员/演员/政治人物/网红）| 趋势词为人名 | `[人名] birth chart` / `[人名] zodiac sign` / `[人名] rising sign` | `Harry Kane birth chart` | 路径①② | 长青 |
| **体育赛事** | 趋势词包含赛事关键词（见附录 A）| `[赛事] astrology` / `zodiac signs as [队伍]` / `[赛事] astrology predictions` | `England World Cup 2026 astrology` | 路径①② | 短时 |
| **影视作品/角色** | 趋势词为电影/剧集/角色名 | `[角色名] zodiac sign` / `[作品] characters zodiac signs` | `Troy Story 5 zodiac signs` | 路径①② | 中长期 |
| **文化事件/颁奖** | 趋势词包含文化事件关键词（见附录 B）| `[事件] astrology` / `[事件] zodiac predictions` | `Oscars 2026 astrology` | 路径①② | 短时 |
| **游戏角色/游戏发布** | 趋势词为游戏名或角色名，角色有官方设定生日优先 | `[角色名] zodiac sign` / `[游戏名] characters zodiac signs` | `GTA 6 characters zodiac signs` | 路径①② | 中长期 |
| **政治选举/重大政治事件** | 趋势词包含选举、候选人、政治事件关键词（见附录 C）| `[选举/事件] astrology prediction` / `[候选人] birth chart` | `2026 midterms astrology prediction` | **仅路径①（astrologywiki）** ⚠️ | 短时 |

> ⚠️ **政治类内容限制**：政治相关页面属于 Google AdSense 敏感类目，会被降低 CPM 或限制广告投放。路径B新站以 AdSense 变现为目标，**不得**使用政治类桥接。仅在 astrologywiki 上产出，且内容必须从星盘/行星运行角度分析，不得发表政治立场或背书候选人。

**内容时效说明：**
- **长青**：写完后持续排名，质量优先，48小时目标可适当放宽至72小时
- **中长期**：作品/游戏热度维持数月至数年，正常48小时目标
- **短时**：赛事/颁奖窗口仅数天，必须24小时内发布，否则放弃

---

## 五、出生日期验证（热门人物专用）

热门人物桥接需要已知出生日期才能写 birth chart 内容。判断流程：

1. 查询 Wikidata API，检查 `date of birth`（P569）和 `place of birth`（P19）字段
2. **有完整出生日期 + 出生地** → 最优，公式用 `[人名] birth chart`，可写完整星盘
3. **有出生日期，无出生时间**（绝大多数公众人物）→ 公式用 `[人名] birth chart`，内容注明"上升星座待确认"，只分析太阳/月亮/行星位置
4. **仅知出生年份或星座** → 降级，公式改为 `[人名] zodiac sign`，Path ① 仍通过
5. **完全无生日信息** → Path ① 不通过，继续判断 Path ②

> 出生时间缺失是常态，不作为否决条件，但须在通知中标注，让内容负责人提前知道内容深度上限。

---

## 六、Path ① 通过后的输出

Module 1.5 判定为 Path ① 后，推送给内容负责人的通知需包含：

| 字段 | 内容 |
|---|---|
| 原始趋势词 | 如 `Harry Kane` |
| 桥接类型 | 如 `热门人物` |
| 推荐内容关键词（候选）| 如 `Harry Kane birth chart` / `Harry Kane zodiac sign` / `Harry Kane rising sign` |
| 内容时效 | 如 `长青`（决定发布优先级）|
| 出生日期 | 如 `1993-07-28`（Wikidata 自动获取）|
| 出生时间 | 如 `未知`（影响内容深度，标注说明）|
| 出生地 | 如 `Galway, Ireland`（Wikidata 自动获取）|
| 桥接词 SERP 评估 | 如 `前10平均DR 32，有小站内页，✅ 可做` |
| Google Trends 估算搜索量 | 如 `500K+` |
| Trending Now 发现时间 | 如 `2026-07-06 14:00` |

内容负责人收到通知后按现有内容 SOP 产出文章，发布时限参考内容时效字段。

---

## 七、Watchlist 预置机制（可选，提升响应速度）

部分桥接机会可以提前预判（赛事参赛名单、颁奖提名名单、大选候选人），在趋势出现前提前完成 Wikidata 查询和 SERP 评估，存入 Watchlist。

触发逻辑：Module 1 发现候选词 → 先查 Watchlist → 命中则跳过 Step 4（出生日期验证），直接输出通知，响应速度提升约 2 分钟。

Watchlist 维护：每季度更新一次，由内容负责人手动维护（世界杯赛季前补充球员名单，颁奖季前补充提名名单）。

---

## 八、附录 A：体育赛事关键词列表

```
world cup, copa america, euro, champions league, premier league,
nba finals, super bowl, wimbledon, olympics, grand slam,
la liga, bundesliga, serie a, nfl, mlb, nhl
```

---

## 九、附录 B：文化事件/颁奖关键词列表

```
oscars, grammy, met gala, golden globes, bafta,
coachella, super bowl halftime, vma, billboard awards
```

---

## 十、附录 C：政治选举/事件关键词列表（仅 astrologywiki）

```
election, midterms, primary, inauguration, referendum,
senate, congress, president, prime minister, parliament
```

---

*文件：inbox/02-keyword-research/2026-07-06-趋势词桥接逻辑-v1.0.md*
*版本：v1.1 | 2026-07-06*
*交付对象：后端同事（路径B自动化建站系统维护人）*
