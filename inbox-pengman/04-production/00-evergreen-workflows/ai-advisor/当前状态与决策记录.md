---
title: 当前状态与决策记录
project: astrologywiki
type: current-status-and-decisions
status: active
owner: Pengman
current_date: 2026-08-04
week: 2026-W32
updated: 2026-08-04
update_mode: overwrite-current-state
---

# 当前状态与决策记录

> 这是滚动状态文件，不是历史档案。后续更新应覆盖已经失效的周次、状态和决定，不无限追加旧周内容。

## 1. 当前周期

- 当前日期：2026-08-04
- 当前周：2026-W32（2026-08-03 至 2026-08-09）
- 当前模式：**加速恢复周 / 同周生产发布临时例外**
- 例外原因：W31 健康请假导致断更超过一周，W32 暂时把恢复发布频率置于库存缓冲之前。
- 长期默认机制仍是滚动周：本周发布上周库存，本周生产下周内容。
- 当前周证据：`inbox-pengman/04-production/03-weekly-content-plans/2026-W32 周度内容计划.md`

## 2. 本周目标和产能

- 可用总时间：22 小时。
- 计划产能：`4S + 4M`，无 L 项。
- 发布目标：6 条；其中 `@miraaastrology` 4 条，`@astrologywiki` 2 条。
- 库存目标：为 W33 制作 2 条官方账号 Photo 库存。
- Hot 槽：只有事件达到现行评分门槛时才触发；当前没有已完成核验且达到门槛的 Hot 候选。
- 当前生产工具：HeyGen 网页端；不把 API 实验视为正式自动化。

以上均为 W32 临时恢复安排，不构成长期账号配额或长期同周生产发布规则。

## 3. 当前内容队列

以下阶段来自 2026-08-04 重新读取的单条主生产记录，而不是从周计划推算。

| content_id | 账号 | 当前 `content_stage` | 发布证据 | 当前状态或下一步 |
|---|---|---|---|---|
| `aw-astrologywiki-photo-mars-cancer-2026` | ① `@astrologywiki` | `published` | 已回写真实链接、平台 ID 和发布时间（publish_sync 自动匹配） | 主记录 `published_url / platform_post_id / published_at` 完整，发布证据可核验 |
| `aw-scorpio-psychology-ai-host-07` | ② `@miraaastrology` | `ready` | 无发布链接 | 已有成片且 Pengman 表示已发；按证据门仍需补真实链接后才能进入 `published` |
| `aw-scorpio-leo-pair-01` | ② `@miraaastrology` | `published` | 已回写真实链接、平台 ID 和发布时间（publish_sync 自动匹配） | 主记录 `published_url / platform_post_id / published_at` 完整，发布证据可核验 |
| `aw-scorpio-virgo-pair-01` | ② `@miraaastrology` | `ready` | 定时信息已写入，公开链接待发布后补 | 已生成并定时至 `2026-08-04T07:00:00-05:00`；定时状态由 `scheduled_at` 表示 |
| `aw-astrologywiki-short-leo-eclipse-2026` | ① `@astrologywiki` | `selected` | 不适用 | 日期、度数和安全边界已核验；下一步为 chart CTA Brief |
| `aw-astrologywiki-photo-venus-libra-relationship` | ① `@astrologywiki` | `selected` | 不适用 | `inventory_ready: false`；本周目标为 `ready + inventory_ready: true` |
| `aw-astrologywiki-photo-mercury-leo-voice` | ① `@astrologywiki` | `selected` | 不适用 | `inventory_ready: false`；备用发布日期不是已确认排期，产能不足时可优先削减 |

已归入 `07-content-production/已发布/` 的 August Forecast、Scorpio × Cancer #02、Scorpio Psychology #06 均有真实 `published_url`、平台 ID 和发布时间；它们是既有发布证据，不属于当前未完成队列。

`aw-scorpio-psychology-ai-host-11` 出现在 W32 周计划中，但截至本次复核未发现对应单条主生产记录，因此不将其写成已经进入实际生产队列。

## 4. 已确认决定

### 决定 1：W32 使用临时恢复模式

- `decision`：W32 采用加速恢复周和同周生产发布例外；长期默认仍为滚动周。
- `decided_at`：2026-08-03
- `evidence`：`inbox-pengman/04-production/03-weekly-content-plans/2026-W32 周度内容计划.md`
- `owner`：Pengman
- `review_at`：2026-08-07 周五复盘；2026-W33 建计划时恢复按标准周判断

### 决定 2：本周集中恢复 Miraa 发布频率

- `decision`：W32 以 `@miraaastrology` 4 条 AI Host 为主力，官方账号发布 2 条时效内容，并制作 2 条官方账号库存。
- `decided_at`：2026-08-03
- `evidence`：W32 周计划的 Week Setup、Publishing This Week 和 Active Account Allocation
- `owner`：Pengman
- `review_at`：2026-08-07；结合实际发布数和 24h/72h 数据复核

### 决定 3：当前只启用两个生产账号

- `decision`：当前只聚焦 `@miraaastrology` 与 `@astrologywiki`；`@filestarsx` 暂停，历史普通爱好者号退役。未来账号必须先通过启用门。
- `decided_at`：2026-08-04
- `evidence`：Pengman 当前确认；`inbox-pengman/04-production/01-strategy-and-platform-research/AstrologyWiki 社媒账号定位与内容路由 Playbook.md`
- `owner`：Pengman
- `review_at`：提议恢复或新增账号时

### 决定 4：不恢复旧每日候选机制

- `decision`：不每天从零为所有历史账号生成候选，不自动把候选提升为 `selected`，不因缺少 Hot 推翻周一计划。
- `decided_at`：2026-07-20 起生效；W32 继续有效
- `evidence`：`inbox-pengman/04-production/00-evergreen-workflows/weekly-rolling-content-production-sop.md` 与 `inbox-pengman/AGENTS.md`
- `owner`：Pengman
- `review_at`：仅在 Pengman 明确重排并更新权威 SOP 时复核

### 决定 5：生产状态以主记录为准

- `decision`：使用单条主生产记录的 `content_stage` 判断实时状态；周计划只说明计划目标。没有真实 URL 的 `published` 不算完整核验发布。
- `decided_at`：现行长期规则；本文件于 2026-08-03 复核
- `evidence`：`inbox-pengman/AGENTS.md`、滚动周 SOP、`inbox-pengman/04-production/07-content-production/`
- `owner`：Pengman
- `review_at`：持续有效

### 决定 6：自动化按证据成熟度描述

- `decision`：TikTok 公共采集可表述为有运行证据；HeyGen 网页端是当前人工生产工具；HeyGen API、Feishu Bot、Buffer 和其他实验不自动表述为稳定日常自动化。
- `decided_at`：2026-08-03 状态复核
- `evidence`：`inbox-pengman/04-production/00-evergreen-workflows/tiktok-public-capture/README.md`、本地运行日志和数据库、W32 周计划及现有调研报告
- `owner`：Pengman
- `review_at`：相关系统出现新的部署、验收或运行证据时

## 5. 已暂停或废弃事项

- `@filestarsx`：暂停生产，不预留 Hot 配额；历史数据可继续保留。
- 原普通占星爱好者账号：已退役，不再作为当前生产或低成本测试入口。
- MBTI × Sun Sign 新账号：继续暂缓，不占 W32 产能。
- 录屏 + Codex canary：W32 不占产能，留在 Idea/实验池。
- `tools/internal/skills/social-daily/SKILL.md`：不再是当前执行入口，不恢复每日批量生成机制。
- W31 未执行内容不机械顺延；只有重新核验并被当前周计划选中的内容进入 W32。
- GSC 输入继续暂停。

## 6. 当前阻塞

| 阻塞 | 影响 | 当前证据 | 处理要求 |
|---|---|---|---|
| Scorpio #07 缺真实发布链接 | 主记录只能保持 `ready`，无法完整核验发布 | `aw-scorpio-psychology-ai-host-07.md` | 补 `published_url`、平台 ID 和实际发布时间后进入 `published` |
| Scorpio #11 无单条主记录 | 周计划中存在，但实际阶段无法确认 | W32 周计划与 `07-content-production/` 文件清单 | 按周计划先完成去重、标题/角度和建主记录，再判断阶段 |
| W33 发布级库存不足 | 下周仍可能依赖同周生产 | W32 周计划与两条库存记录均为 `selected`、`inventory_ready: false` | 在现有 W32 库存任务范围内完成 Venus 与 Mercury，不新增额外任务 |

## 7. 当前信息缺口

- Scorpio #07 应重生成还是调整参数，以及新的实际 credits 消耗。
- Scorpio #11 的最终角度、标题和单条主记录。
- 暂停/退役账号是否继续纳入公共数据采集（若保留，仅表示数据连续性，不表示恢复生产）。
- Venus Libra、Mercury Leo 是否能在 W32 达到 `ready + inventory_ready: true`。
- TikTok retention、profile visits、link clicks、单帖增粉和站外转化数据。
- Feishu Bot、Buffer、HeyGen API、Goose、Cherry Studio 的当前正式状态。

## 8. 接下来三个最重要动作

1. **补齐 Scorpio #07 与 × Virgo 的公开证据**：公开后回写真实 URL、平台 ID 和实际发布时间；证据齐全后从 `ready` 进入 `published`。
2. **（已完成）Mars Cancer 与 Scorpio × Leo 发布证据已回写**：tiktok-public-capture 已于 2026-08-04 02:05 UTC 自动匹配并写入 `published_url / platform_post_id / published_at`，发布状态可核验，无需再人工补链接。
3. **补全 W32 已计划但缺失的 #11 主记录**：按现有周计划完成去重、确定角度/标题并建立单条主记录；不得把周计划中的预期阶段直接当作实际阶段。

## 9. 更新规则

- 本文件只保存当前仍有效的状态和决定。
- 新一周开始时覆盖日期、周模式、目标、队列、阻塞、缺口和下一步，不追加旧周流水。
- 周一更新当周产能、组合和例外；周五更新实际发布、库存、数据和继续/调整/停止决定。
- 每次更新先读取当前周计划和对应单条主生产记录。
- 历史证据留在周计划、单条主记录、发布合集和周报，不复制到本文件中形成第二套档案。
