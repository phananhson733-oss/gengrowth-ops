# 短剧发行管理 v5 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在目标 Mac mini 上交付由现有 Social Bot 操作的短剧发行管理系统，以飞书 Base 四表为正式业务入口，以 SQLite 为历史事实源，并安全迁移现有 Google 业务数据。

**Architecture:** 在 `gengrowth-ops` 中建立无常驻服务的 Node.js CLI `shortdrama_ctl`，负责 Job/Audit、单实例锁、四表 Base Adapter、迁移、同步和通知；现有 TikTok Collector 保持独立并由 Runner 调用。Hermes 侧只新增短剧 Social Skill 和运行时预授权，不增加核心工具或改变现有 AstrologyWiki Social OS。

**Tech Stack:** Node.js 24+、`node:test`、`node:sqlite`、飞书 OpenAPI、多维表格四表关系、现有 Hermes Python/pytest Skill 合约、macOS launchd。

## Global Constraints

- 需求 SSOT：`inbox-pengman/06-requirements/2026-08-30-短剧数据采集迁移与发行管理平台需求文档.md` v5.0。
- 正式 Base 固定四表：`账号台账 / 发布记录 / 选剧池 / 采集数据`；不得增加日历史快照表。
- `采集数据`按 Post ID 保存 SQLite 最新帖子投影；日历史只留 `tiktok_metrics.sqlite`。
- `选剧池`人工维护；采集器、定时任务和自动推荐不得修改其人工字段。
- 现有 TikTok Daily Metrics、AstrologyWiki Social OS、粉丝同步和生产记录回填保持不变。
- 不复用旧 `sync_shortdrama_to_feishu.mjs --sync` 的全表删除重建行为。
- 不新增 Hermes 核心工具，不新增常驻 HTTP 服务，不让 Skill 直接拼裸 Base API。
- Node 运行时最低版本 24；不增加 npm 生产依赖，优先使用 Node 标准库。
- Base 同表写入串行，单批最多 200 条；限流最多重试 3 次并优先遵守 `Retry-After`。
- 业务日期使用 `Asia/Shanghai`；TikTok 原始发布时间保留 ISO 时区。
- `SD-000001 / SR-000001 / SDRUN-YYYYMMDD-HHMMSS` 是稳定可读 ID。
- 人工值优先；缺失是 `null`，真实零是 `0`；机器失败不得清空旧有效值。
- 所有写入必须读回；分页不完整和字段不可用必须返回 `partial/unavailable`，不得报告有效零。
- 只有实名 actor 可写和触发采集；普通群成员只读；schema/migration apply 仅 privileged。
- 凭证只存目标运行环境 `.env`，不得进入 Git、日志、测试夹具或回复。
- 旧 Google 业务表只在七天切换时改为只读；TikTok Daily Metrics 不退役。
- Base 创建、migration apply、权限变化、launchd 安装、Social gateway 重启和 Google 只读切换均需动作时确认。
- 不在当前脏的 `/Users/awayer_mini/hermes-agent` 工作树直接实施；执行时必须先使用 `superpowers:using-git-worktrees` 创建隔离工作树。

## Repository And Worktree Strategy

| 工作流 | 仓库 | 分支 | 允许修改范围 |
|---|---|---|---|
| Ops Runner | `/Users/awayer_mini/gengrowth-ops` | `codex/shortdrama-runner` | `inbox-pengman/tools/short-drama-release-manager/**`、必要的 `.env.example` 和需求文档 |
| Social Skill | `/Users/awayer_mini/hermes-agent` | `codex/shortdrama-social-skill` | `skills/social-media/short-drama-release-manager/**`、对应 `tests/skills/**` |
| Live rollout | `/Users/awayer_mini/.hermes/profiles/social` | 非 Git | 先备份，只部署已验证 Skill/SOUL 片段，针对性重启 social gateway |

Ops 与 Hermes 两条代码线分别提交和评审；生产部署不得从未提交文件复制。`gengrowth-ops` 主工作区存在 Vault 自动提交，代码实施必须在独立 worktree 中完成，避免后台任务替代理应明确的原子提交。

执行时统一使用以下绝对路径和变量；不得自行换成未记录的临时位置：

```bash
export OPS_REPO=/Users/awayer_mini/gengrowth-ops
export HERMES_REPO=/Users/awayer_mini/hermes-agent
export OPS_WORKTREE=/Users/awayer_mini/.codex/worktrees/gengrowth-ops-shortdrama-runner
export HERMES_WORKTREE=/Users/awayer_mini/.codex/worktrees/hermes-agent-shortdrama-social-skill
export RUNNER_DIR="$OPS_WORKTREE/inbox-pengman/tools/short-drama-release-manager"
export HERMES_SKILL_SOURCE="$HERMES_WORKTREE/skills/social-media/short-drama-release-manager"
export OPS_BASE_COMMIT="$(git -C "$OPS_REPO" rev-parse HEAD)"
export HERMES_BASE_COMMIT="$(git -C "$HERMES_REPO" rev-parse HEAD)"
export RUNTIME_CONFIG="$RUNNER_DIR/shortdrama.runtime.json"
export EVIDENCE_DIR="$OPS_WORKTREE/inbox-pengman/output/short-drama-release-manager/acceptance/$(date +%Y%m%d-%H%M%S)"
```

`shortdrama.runtime.json`和 acceptance 目录必须被 Git 忽略。Task 12 开始前，从已创建的新 Base 和实名 privileged 操作者取得并导出：

```bash
export SHORTDRAMA_NEW_BASE_URL="$(jq -r '.base.url' "$RUNTIME_CONFIG")"
export PRIVILEGED_ACTOR_ID="$(jq -r '.acceptance.privileged_actor_id' "$RUNTIME_CONFIG")"
mkdir -p "$EVIDENCE_DIR"
```

## Planned File Structure

### `/Users/awayer_mini/gengrowth-ops`

```text
inbox-pengman/tools/short-drama-release-manager/
├── package.json                         # Node 版本与统一测试入口
├── README.md                            # 新 Runner 使用说明；旧脚本标记 historical
├── shortdrama.config.example.json       # 非秘密资源与运行策略示例
├── shortdrama_ctl.mjs                   # 唯一 CLI 入口和固定命令分派
├── run_scheduled.sh                     # 北京时间 08:00 幂等 ticker
├── install_launchd.sh                   # 可恢复安装、read-back 和健康检查
├── launchd/
│   └── com.gengrowth.shortdrama-sync.plist
├── src/
│   ├── errors.mjs                       # 命名错误与公共 JSON 错误形状
│   ├── config.mjs                       # 配置、env、actor/chat allowlist 校验
│   ├── schema.mjs                       # 四表 schema、字段所有权和枚举 SSOT
│   ├── ids.mjs                          # SD/SR/SDRUN ID 分配
│   ├── job-store.mjs                    # shortdrama_ops.sqlite Job/Audit
│   ├── run-lock.mjs                     # 跨定时/手动入口单实例锁
│   ├── feishu-client.mjs                # Token、分页、200 条批次、重试和脱敏
│   ├── base-repositories.mjs            # 四表读写、差异 patch 和读回
│   ├── source-sqlite.mjs                # tiktok_metrics.sqlite 最新投影
│   ├── matcher.mjs                      # URL > Post ID > 唯一时间窗
│   ├── human-ops.mjs                    # 查询、单字段写、preview/apply 和归档
│   ├── google-source.mjs                # 一次性迁移源和备份读取
│   ├── migration.mjs                    # plan/apply/verify manifest
│   ├── notifier.mjs                     # 只向 allowlist 原会话发终态
│   └── sync-runner.mjs                  # Collector → SQLite → Base → read-back
└── tests/
    ├── schema-config.test.mjs
    ├── ids-job-lock.test.mjs
    ├── feishu-client.test.mjs
    ├── base-repositories.test.mjs
    ├── source-matcher.test.mjs
    ├── human-ops.test.mjs
    ├── migration.test.mjs
    ├── sync-runner.test.mjs
    ├── cli.test.mjs
    └── launchd.test.mjs
```

保留但不再作为生产入口：

```text
sync_shortdrama_to_feishu.mjs
audit_platform.mjs
build_workbook.mjs
launchd/com.gengrowth.shortdrama-feishu-sync.plist
```

### `/Users/awayer_mini/hermes-agent`

```text
skills/social-media/short-drama-release-manager/
├── SKILL.md
└── references/
    ├── response-contract.md
    └── soul-preauthorization.md
tests/skills/
└── test_short_drama_release_manager_skill.py
```

### Live profile

```text
/Users/awayer_mini/.hermes/profiles/social/
├── SOUL.md                               # 增加第三条产品级预授权边界
└── skills/social-media/short-drama-release-manager/
    ├── SKILL.md
    └── references/response-contract.md
```

---

### Task 1: 锁定四表 Schema、配置和错误协议

**Files:**
- Create: `inbox-pengman/tools/short-drama-release-manager/package.json`
- Create: `inbox-pengman/tools/short-drama-release-manager/shortdrama.config.example.json`
- Create: `inbox-pengman/tools/short-drama-release-manager/src/errors.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/src/config.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/src/schema.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/tests/schema-config.test.mjs`

**Interfaces:**
- Produces: `TABLES`, `TABLE_ORDER`, `BASE_FIELD_SPECS`, `fieldOwner(table, field)`, `assertPatchAllowed(table, patch, actorKind)`, `loadRuntimeConfig(options)`。
- Produces errors: `ShortDramaError(code, message, details)` and `toErrorResult(error)`。
- Consumed by: Tasks 2–9。

- [ ] **Step 1: 创建隔离 worktree 和分支**

使用 `superpowers:using-git-worktrees` 在不被 Vault 自动同步扫描的位置创建 `gengrowth-ops` worktree，分支固定为 `codex/shortdrama-runner`。确认：

```bash
git status --short --branch
git diff --name-only
```

Expected: 新 worktree 干净，当前分支为 `codex/shortdrama-runner`。

- [ ] **Step 2: 写 schema/config 红测**

创建 `tests/schema-config.test.mjs`：

```js
import assert from "node:assert/strict";
import test from "node:test";

import { ShortDramaError, toErrorResult } from "../src/errors.mjs";
import {
  TABLE_ORDER,
  TABLES,
  assertPatchAllowed,
  fieldOwner,
} from "../src/schema.mjs";
import { loadRuntimeConfig } from "../src/config.mjs";

test("schema fixes the four Base tables and source ownership", () => {
  assert.deepEqual(TABLE_ORDER, ["账号台账", "选剧池", "采集数据", "发布记录"]);
  assert.equal(TABLES["采集数据"].primaryField, "Post ID");
  assert.equal(fieldOwner("选剧池", "推荐理由"), "human");
  assert.equal(fieldOwner("采集数据", "播放量"), "machine");
  assert.equal(fieldOwner("发布记录", "播放量"), "derived");
  assert.equal(fieldOwner("发布记录", "Post ID"), "shared");
});

test("machine patches cannot touch human fields", () => {
  assert.throws(
    () => assertPatchAllowed("选剧池", { 推荐理由: "自动生成" }, "machine"),
    (error) => error.code === "field_owner_violation"
  );
  assert.doesNotThrow(() =>
    assertPatchAllowed("采集数据", { 播放量: 0, 点赞: null }, "machine")
  );
  assert.doesNotThrow(() =>
    assertPatchAllowed("发布记录", { "Post ID": "99" }, "human")
  );
  assert.doesNotThrow(() =>
    assertPatchAllowed("发布记录", { "Post ID": "99" }, "machine")
  );
  assert.doesNotThrow(() =>
    assertPatchAllowed("账号台账", { 所属组: "A纯切片", 粉丝数: 1161 }, "migration")
  );
  assert.throws(
    () => assertPatchAllowed("发布记录", { 播放量: 20 }, "migration"),
    (error) => error.code === "field_owner_violation"
  );
});

test("runtime config rejects missing secrets and unknown notification chats", () => {
  assert.throws(
    () => loadRuntimeConfig({ env: {}, config: {} }),
    (error) => error.code === "config_invalid"
  );
});

test("errors have stable public JSON", () => {
  const result = toErrorResult(new ShortDramaError("base_schema_drift", "字段漂移", {
    table: "采集数据",
  }));
  assert.deepEqual(result, {
    status: "failed",
    error: {
      code: "base_schema_drift",
      message: "字段漂移",
      details: { table: "采集数据" },
    },
  });
});
```

- [ ] **Step 3: 运行红测**

Run:

```bash
node --test inbox-pengman/tools/short-drama-release-manager/tests/schema-config.test.mjs
```

Expected: FAIL with `ERR_MODULE_NOT_FOUND` for `src/schema.mjs`。

- [ ] **Step 4: 实现 errors/config/schema**

`src/errors.mjs`：

```js
export class ShortDramaError extends Error {
  constructor(code, message, details = {}) {
    super(message);
    this.name = "ShortDramaError";
    this.code = code;
    this.details = details;
  }
}

export function toErrorResult(error) {
  const known = error instanceof ShortDramaError;
  return {
    status: "failed",
    error: {
      code: known ? error.code : "internal_error",
      message: known ? error.message : "Internal short-drama runner error",
      details: known ? error.details : {},
    },
  };
}
```

`src/schema.mjs` 必须完整声明以下字段所有权：

```js
import { ShortDramaError } from "./errors.mjs";

const table = (primaryField, human, machine, shared, derived, options = {}) => ({
  primaryField,
  human: new Set(human),
  machine: new Set(machine),
  shared: new Set(shared),
  derived: new Set(derived),
  options,
});

export const TABLE_ORDER = ["账号台账", "选剧池", "采集数据", "发布记录"];

export const TABLES = {
  "账号台账": table(
    "账号ID",
    ["账号名", "所属组", "定位垂类", "表现形式", "状态"],
    ["账号ID", "粉丝数", "数据日期", "指标同步时间", "同步状态"],
    ["主页链接"],
    []
  ),
  "选剧池": table(
    "剧ID",
    [
      "剧名", "剧分类", "上线日期", "生命周期", "备注", "推荐理由",
      "RS Boost 分类（待确认）", "账号组", "账号状态", "平台", "语言",
      "来源", "推荐人", "归档状态",
    ],
    ["剧ID"],
    [],
    ["是否已排期", "关联发布记录", "创建人", "创建时间", "最后修改时间"],
    {
      platform: ["ReelShort", "DramaBox", "ShortMax", "TopShort", "其他"],
      recommender: ["彭满", "高璇", "马博洋"],
    }
  ),
  "采集数据": table(
    "Post ID",
    [],
    [
      "Post ID", "快照日期", "采集时间", "账号", "视频链接", "发布时间",
      "播放量", "点赞", "评论", "收藏", "转发", "业务", "采集状态",
      "缺失字段", "来源 run_id", "Base 同步时间",
    ],
    [],
    ["账号名", "关联发布记录"]
  ),
  "发布记录": table(
    "发布ID",
    ["日期", "账号", "剧", "剧ID（RS Boost）", "RS收益", "备注", "归档状态"],
    ["发布ID", "采集记录", "匹配方式", "匹配置信度", "指标同步时间", "同步错误"],
    ["视频链接", "Post ID"],
    ["账号名", "主页链接", "剧ID", "剧名", "剧分类", "播放量", "点赞", "收藏", "转发", "评论", "发布状态", "指标日期"]
  ),
};

export function fieldOwner(tableName, field) {
  const definition = TABLES[tableName];
  if (!definition) throw new ShortDramaError("table_not_allowed", "Unknown table", { table: tableName });
  for (const owner of ["human", "machine", "shared", "derived"]) {
    if (definition[owner].has(field)) return owner;
  }
  throw new ShortDramaError("field_not_allowed", "Unknown field", { table: tableName, field });
}

export function assertPatchAllowed(tableName, patch, actorKind) {
  for (const field of Object.keys(patch)) {
    const owner = fieldOwner(tableName, field);
    const allowed =
      (actorKind === "migration" && ["human", "machine", "shared"].includes(owner)) ||
      owner === actorKind ||
      (owner === "shared" && ["human", "machine"].includes(actorKind));
    if (!allowed) {
      throw new ShortDramaError("field_owner_violation", "Field owner mismatch", {
        table: tableName,
        field,
        expected_owner: owner,
        actor_kind: actorKind,
      });
    }
  }
}
```

`src/config.mjs` 必须：读取 JSON config 和显式 env 对象；验证 Node 主版本 ≥24；解析四个真实 table ID、SQLite 路径、Collector 路径、Ops DB 路径、actor allowlist、privileged allowlist 和 notification chat allowlist；缺少任何生产字段时抛 `config_invalid`；绝不返回 app secret 给日志层。

`shortdrama.config.example.json`固定为非生产示例，所有秘密只引用 env key：

```json
{
  "schema_version": "shortdrama/v1",
  "timezone": "Asia/Shanghai",
  "source_spreadsheet_id": "1BbOcWUVrhRsnuSAs9LcyCuYWTrauPxtJWI12Esao7p0",
  "paths": {
    "metrics_sqlite": "../../output/tiktok_metrics.sqlite",
    "collector": "../tiktok-public-capture/collect_tiktok_public_data.mjs",
    "collector_summary_dir": "../../output",
    "ops_sqlite": "../../output/short-drama-release-manager/shortdrama_ops.sqlite",
    "run_lock": "../../output/short-drama-release-manager/run.lock",
    "payload_root": "../../output/short-drama-release-manager/payloads"
  },
  "base": {
    "url": "https://example.invalid/base/company-owned-short-drama",
    "app_token_env": "FEISHU_SHORTDRAMA_APP_TOKEN",
    "table_id_envs": {
      "accounts": "FEISHU_SHORTDRAMA_ACCOUNTS_TABLE_ID",
      "dramas": "FEISHU_SHORTDRAMA_POOL_TABLE_ID",
      "captures": "FEISHU_SHORTDRAMA_CAPTURES_TABLE_ID",
      "releases": "FEISHU_SHORTDRAMA_RELEASES_TABLE_ID"
    }
  },
  "auth": {
    "feishu_app_id_env": "FEISHU_APP_ID",
    "feishu_app_secret_env": "FEISHU_APP_SECRET",
    "google_service_account_path_env": "GOOGLE_SERVICE_ACCOUNT_JSON",
    "operator_ids_env": "SHORTDRAMA_OPERATOR_IDS",
    "privileged_ids_env": "SHORTDRAMA_PRIVILEGED_IDS",
    "notification_chat_ids_env": "SHORTDRAMA_NOTIFICATION_CHAT_IDS"
  },
  "acceptance": {
    "privileged_actor_id": "ou_example_not_production"
  }
}
```

`example.invalid`和 `ou_example_not_production`必须被 `loadRuntimeConfig`在生产模式拒绝；示例文件永远不能被 Runner 当作 runtime config。

`BASE_FIELD_SPECS`必须把 v5.0 第 6 节每个字段映射为唯一具体 kind：`text / number / url / date / datetime / single_select / multi_select / link / lookup / formula / system`。所有 relation 还要写 `targetTable`，所有 lookup 写 `linkField/sourceField`，发布状态和是否已排期写完整 formula 表达式；不得在实现时临时猜字段类型。Schema apply 顺序固定为存储字段 → link → lookup/formula → system/view/dashboard。

`package.json`：

```json
{
  "name": "gengrowth-short-drama-release-manager",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check shortdrama_ctl.mjs"
  }
}
```

- [ ] **Step 5: 运行绿测**

Run:

```bash
node --test inbox-pengman/tools/short-drama-release-manager/tests/schema-config.test.mjs
```

Expected: 4 tests PASS, 0 FAIL。

- [ ] **Step 6: 原子提交**

```bash
git add \
  inbox-pengman/tools/short-drama-release-manager/package.json \
  inbox-pengman/tools/short-drama-release-manager/shortdrama.config.example.json \
  inbox-pengman/tools/short-drama-release-manager/src/errors.mjs \
  inbox-pengman/tools/short-drama-release-manager/src/config.mjs \
  inbox-pengman/tools/short-drama-release-manager/src/schema.mjs \
  inbox-pengman/tools/short-drama-release-manager/tests/schema-config.test.mjs
git commit -m "feat(short-drama): define Base schema and safety contract"
```

---

### Task 2: 实现稳定 ID、Job/Audit 和单实例锁

**Files:**
- Create: `inbox-pengman/tools/short-drama-release-manager/src/ids.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/src/job-store.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/src/run-lock.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/tests/ids-job-lock.test.mjs`

**Interfaces:**
- Consumes: `ShortDramaError`。
- Produces: `allocateBusinessId(db, "drama"|"release")`, `makeRunId(date)`, `JobStore`, `acquireRunLock(path, runId)`。
- Job states: `queued → running → success|partial|failed`; terminal states immutable。
- Preview interface: `createPreview`, `getPreview`, `consumePreview`；receipt 绑定 actor/chat/action/target/before_hash/patch，15 分钟、单次使用。

- [ ] **Step 1: 写 ID/Job/lock 红测**

```js
import assert from "node:assert/strict";
import { mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import test from "node:test";
import { DatabaseSync } from "node:sqlite";

import { allocateBusinessId, makeRunId } from "../src/ids.mjs";
import { JobStore } from "../src/job-store.mjs";
import { acquireRunLock } from "../src/run-lock.mjs";

test("business IDs are monotonic and readable", () => {
  const db = new DatabaseSync(":memory:");
  assert.equal(allocateBusinessId(db, "drama"), "SD-000001");
  assert.equal(allocateBusinessId(db, "drama"), "SD-000002");
  assert.equal(allocateBusinessId(db, "release"), "SR-000001");
});

test("run IDs use Beijing wall time", () => {
  assert.equal(makeRunId(new Date("2026-09-01T00:00:01Z")), "SDRUN-20260901-080001");
});

test("job terminal state cannot be rewritten", () => {
  const store = new JobStore(":memory:");
  const job = store.create({ runId: "SDRUN-20260901-080001", trigger: "manual", actorId: "ou_test", chatId: "oc_test" });
  store.transition(job.run_id, "running", { step: "collector" });
  store.transition(job.run_id, "success", { accounts_updated: 11 });
  assert.throws(() => store.transition(job.run_id, "failed", {}), /terminal/);
});

test("preview receipt is actor-bound and single-use", () => {
  const store = new JobStore(":memory:");
  const receipt = store.createPreview({
    receiptId: "sdp_test",
    actorId: "ou_operator",
    chatId: "oc_social",
    action: "update",
    targetTable: "选剧池",
    targetKey: "SD-000001",
    beforeHash: "before",
    patch: { 推荐理由: "人工补充" },
    now: "2026-09-01T00:00:00Z",
  });
  assert.throws(
    () => store.consumePreview(receipt.receipt_id, { actorId: "ou_other", chatId: "oc_social", beforeHash: "before", now: "2026-09-01T00:01:00Z" }),
    /actor/
  );
  store.consumePreview(receipt.receipt_id, { actorId: "ou_operator", chatId: "oc_social", beforeHash: "before", now: "2026-09-01T00:01:00Z" });
  assert.throws(
    () => store.consumePreview(receipt.receipt_id, { actorId: "ou_operator", chatId: "oc_social", beforeHash: "before", now: "2026-09-01T00:02:00Z" }),
    /used/
  );
});

test("only one run lock can exist", () => {
  const dir = mkdtempSync(join(tmpdir(), "shortdrama-lock-"));
  const first = acquireRunLock(join(dir, "run.lock"), "run-1");
  assert.throws(() => acquireRunLock(join(dir, "run.lock"), "run-2"), (error) => error.code === "already_running");
  first.release();
});
```

- [ ] **Step 2: 运行红测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/ids-job-lock.test.mjs`

Expected: FAIL because the three modules do not exist。

- [ ] **Step 3: 实现 ID 和 Job 数据库**

`ids.mjs` 使用 `id_sequences(kind PRIMARY KEY, last_value)` 和 `BEGIN IMMEDIATE` 分配 `SD/SR`；`makeRunId` 使用 `Intl.DateTimeFormat("en-CA", {timeZone: "Asia/Shanghai", year: "numeric", month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit", second: "2-digit", hourCycle: "h23"})`，不得使用宿主机本地时区。

`job-store.mjs` 建立：

```sql
CREATE TABLE IF NOT EXISTS jobs (
  run_id TEXT PRIMARY KEY,
  trigger TEXT NOT NULL,
  actor_id TEXT,
  chat_id TEXT,
  state TEXT NOT NULL,
  step TEXT NOT NULL,
  started_at TEXT NOT NULL,
  finished_at TEXT,
  counters_json TEXT NOT NULL,
  error_json TEXT NOT NULL,
  notification_state TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS audit_events (
  event_id INTEGER PRIMARY KEY AUTOINCREMENT,
  run_id TEXT,
  actor_id TEXT,
  action TEXT NOT NULL,
  target_table TEXT,
  target_key TEXT,
  before_json TEXT NOT NULL,
  after_json TEXT NOT NULL,
  readback_json TEXT NOT NULL,
  created_at TEXT NOT NULL
);
CREATE TABLE IF NOT EXISTS preview_receipts (
  receipt_id TEXT PRIMARY KEY,
  actor_id TEXT NOT NULL,
  chat_id TEXT NOT NULL,
  action TEXT NOT NULL,
  target_table TEXT NOT NULL,
  target_key TEXT,
  before_hash TEXT NOT NULL,
  patch_json TEXT NOT NULL,
  created_at TEXT NOT NULL,
  expires_at TEXT NOT NULL,
  used_at TEXT
);
```

实现 `create/get/listActive/transition/appendAudit/createPreview/getPreview/consumePreview`；所有 JSON 使用 `JSON.stringify`，终态不可改写，`notification_failed`只更新 notification_state，不改变数据终态。`consumePreview`必须在同一 `BEGIN IMMEDIATE`事务中校验 actor/chat/expiry/unused/before_hash 并写 used_at，防止双击或并发复用。

`run-lock.mjs` 使用 `openSync(lockPath, "wx")` 原子创建，内容固定为 `{run_id,pid,created_at}`；已有锁时读取 Job 状态和 PID：活进程返回 `already_running`，只有进程不存在且 Job 非 running 时才允许 privileged `doctor --repair-stale-lock` 清理，普通 sync 不自动删锁。

- [ ] **Step 4: 运行绿测和 SQLite 完整性检查**

```bash
node --test inbox-pengman/tools/short-drama-release-manager/tests/ids-job-lock.test.mjs
node -e 'const {DatabaseSync}=require("node:sqlite"); const db=new DatabaseSync(":memory:"); console.log(db.prepare("PRAGMA integrity_check").get())'
```

Expected: 5 tests PASS；integrity_check 输出 `ok`。

- [ ] **Step 5: 提交**

```bash
git add inbox-pengman/tools/short-drama-release-manager/src/{ids,job-store,run-lock}.mjs \
  inbox-pengman/tools/short-drama-release-manager/tests/ids-job-lock.test.mjs
git commit -m "feat(short-drama): persist jobs and serialize sync runs"
```

---

### Task 3: 实现 Feishu Client、schema drift 和批量协议

**Files:**
- Create: `inbox-pengman/tools/short-drama-release-manager/src/feishu-client.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/tests/feishu-client.test.mjs`

**Interfaces:**
- Consumes: config secrets and `ShortDramaError`。
- Produces: `FeishuClient` methods `listTables`, `listFields`, `listRecords`, `getRecord`, `createRecords`, `updateRecords`, `createField`, `createTable`, `listViews`, `createView`, `updateView`, `listDashboards`, `createDashboard`, `createDashboardBlock`。
- All list methods return `{items, complete: true, revision}` or throw; no partial list is returned as complete。

- [ ] **Step 1: 写 Client 红测**

```js
import assert from "node:assert/strict";
import test from "node:test";

import { FeishuClient } from "../src/feishu-client.mjs";

test("pagination requires explicit boolean has_more and consumes every page", async () => {
  const responses = [
    { code: 0, data: { items: [{ record_id: "rec1" }], has_more: true, page_token: "p2" } },
    { code: 0, data: { items: [{ record_id: "rec2" }], has_more: false } },
  ];
  const client = new FeishuClient({ tokenProvider: async () => "token", fetchJson: async () => responses.shift() });
  const result = await client.listRecords("app", "tbl");
  assert.deepEqual(result.items.map((item) => item.record_id), ["rec1", "rec2"]);
  assert.equal(result.complete, true);
});

test("malformed pagination fails closed", async () => {
  const client = new FeishuClient({
    tokenProvider: async () => "token",
    fetchJson: async () => ({ code: 0, data: { items: [], has_more: "false" } }),
  });
  await assert.rejects(() => client.listRecords("app", "tbl"), (error) => error.code === "base_response_invalid");
});

test("writes are split into ordered batches of at most 200", async () => {
  const sizes = [];
  const client = new FeishuClient({
    tokenProvider: async () => "token",
    fetchJson: async (_url, options) => {
      sizes.push(options.body.records.length);
      return { code: 0, data: { records: options.body.records } };
    },
  });
  await client.createRecords("app", "tbl", Array.from({ length: 401 }, (_, index) => ({ fields: { Key: String(index) } })));
  assert.deepEqual(sizes, [200, 200, 1]);
});
```

- [ ] **Step 2: 运行红测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/feishu-client.test.mjs`

Expected: FAIL with missing module。

- [ ] **Step 3: 实现 Client**

实现要求：

```js
import { ShortDramaError } from "./errors.mjs";

const batches = (rows, size = 200) =>
  Array.from({ length: Math.ceil(rows.length / size) }, (_unused, index) =>
    rows.slice(index * size, (index + 1) * size)
  );

export class FeishuClient {
  constructor({ tokenProvider, fetchJson, sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms)) }) {
    this.tokenProvider = tokenProvider;
    this.fetchJson = fetchJson;
    this.sleep = sleep;
  }

  async request(path, { method = "GET", body = undefined } = {}) {
    const token = await this.tokenProvider();
    const payload = await this.fetchJson(`https://open.feishu.cn${path}`, {
      method,
      headers: { authorization: `Bearer ${token}`, "content-type": "application/json; charset=utf-8" },
      body,
    });
    if (!payload || payload.code !== 0) {
      throw new ShortDramaError("base_request_failed", "Feishu Base request failed", {
        code: payload?.code ?? null,
        path,
      });
    }
    return payload;
  }

  async listRecords(appToken, tableId) {
    const items = [];
    let pageToken = "";
    do {
      const query = new URLSearchParams({ page_size: "200" });
      if (pageToken) query.set("page_token", pageToken);
      const payload = await this.request(
        `/open-apis/bitable/v1/apps/${encodeURIComponent(appToken)}/tables/${encodeURIComponent(tableId)}/records?${query}`
      );
      const hasMore = payload.data?.has_more;
      if (typeof hasMore !== "boolean") {
        throw new ShortDramaError("base_response_invalid", "has_more must be boolean");
      }
      items.push(...(payload.data?.items ?? []));
      pageToken = hasMore ? String(payload.data?.page_token ?? "") : "";
      if (hasMore && !pageToken) {
        throw new ShortDramaError("base_response_invalid", "page_token missing");
      }
    } while (pageToken);
    return { items, complete: true, revision: null };
  }

  async getRecord(appToken, tableId, recordId) {
    const payload = await this.request(
      `/open-apis/bitable/v1/apps/${encodeURIComponent(appToken)}/tables/${encodeURIComponent(tableId)}/records/${encodeURIComponent(recordId)}`
    );
    return payload.data?.record;
  }

  async createRecords(appToken, tableId, records) {
    const written = [];
    for (const group of batches(records)) {
      const payload = await this.request(
        `/open-apis/bitable/v1/apps/${encodeURIComponent(appToken)}/tables/${encodeURIComponent(tableId)}/records/batch_create`,
        { method: "POST", body: { records: group } }
      );
      written.push(...(payload.data?.records ?? []));
    }
    return written;
  }

  async updateRecords(appToken, tableId, records) {
    const written = [];
    for (const group of batches(records)) {
      const payload = await this.request(
        `/open-apis/bitable/v1/apps/${encodeURIComponent(appToken)}/tables/${encodeURIComponent(tableId)}/records/batch_update`,
        { method: "POST", body: { records: group } }
      );
      written.push(...(payload.data?.records ?? []));
    }
    return written;
  }
}
```

方法体必须：

- 使用 app token/table ID 构造固定 OpenAPI path，不接受完整任意 URL；
- 每页 `page_size<=200`，仅接受布尔 `has_more`；`has_more=true` 时 page_token 必须非空；
- 写入批次按输入顺序切成 200；同表串行；
- 429/1254291 遵守 `Retry-After` 或递增等待，最多 3 次；
- 401/403 映射 `base_auth_failed`，字段类型/缺失映射 `base_schema_drift`；
- JSON 解析失败映射 `base_response_invalid`；
- 日志只记录 method/path/status/run_id，不记录 token、Authorization 或 body 中的自由文本。
- `createField`只能消费 `BASE_FIELD_SPECS`生成的结构；view/dashboard 方法只消费规格 6.5 中固定定义，不能接受用户自由 JSON。

- [ ] **Step 4: 运行绿测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/feishu-client.test.mjs`

Expected: 3 tests PASS。

- [ ] **Step 5: 提交**

```bash
git add inbox-pengman/tools/short-drama-release-manager/src/feishu-client.mjs \
  inbox-pengman/tools/short-drama-release-manager/tests/feishu-client.test.mjs
git commit -m "feat(short-drama): add bounded Feishu Base client"
```

---

### Task 4: 实现四表 Repository、字段所有权和读回

**Files:**
- Create: `inbox-pengman/tools/short-drama-release-manager/src/base-repositories.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/tests/base-repositories.test.mjs`

**Interfaces:**
- Consumes: `TABLES`, `assertPatchAllowed`, `FeishuClient`, real table IDs。
- Produces: `BaseRepositories` with `accounts`, `dramas`, `captures`, `releases` repositories。
- Repository interface: `loadIndex()`, `getByKey(key)`, `upsertByKey(key, patch, actorKind)`, `verify(recordId, expected)`。

- [ ] **Step 1: 写 Repository 红测**

```js
import assert from "node:assert/strict";
import test from "node:test";

import { BaseRepositories } from "../src/base-repositories.mjs";

function fakeClient(seed) {
  const rows = structuredClone(seed);
  return {
    rows,
    async listRecords(_app, tableId) { return { items: rows[tableId] || [], complete: true, revision: "r1" }; },
    async createRecords(_app, tableId, records) {
      rows[tableId] ||= [];
      return records.map((record, index) => {
        const item = { record_id: `rec-new-${index}`, fields: record.fields };
        rows[tableId].push(item);
        return item;
      });
    },
    async updateRecords(_app, tableId, records) {
      for (const patch of records) {
        const item = rows[tableId].find((row) => row.record_id === patch.record_id);
        Object.assign(item.fields, patch.fields);
      }
      return records;
    },
    async getRecord(_app, tableId, recordId) { return rows[tableId].find((row) => row.record_id === recordId); },
  };
}

const tableIds = {
  accounts: "accounts",
  dramas: "dramas",
  captures: "captures",
  releases: "releases",
};

test("capture upsert changes latest machine fields without creating a duplicate", async () => {
  const client = fakeClient({ captures: [{ record_id: "rec1", fields: { "Post ID": "99", 播放量: 10 } }] });
  const repos = new BaseRepositories({ client, appToken: "app", tableIds });
  await repos.captures.upsertByKey("99", { 播放量: 20, 点赞: 0 }, "machine");
  assert.equal(client.rows.captures.length, 1);
  assert.deepEqual(client.rows.captures[0].fields, { "Post ID": "99", 播放量: 20, 点赞: 0 });
});

test("machine update cannot overwrite drama recommendation", async () => {
  const repos = new BaseRepositories({ client: fakeClient({}), appToken: "app", tableIds });
  await assert.rejects(
    () => repos.dramas.upsertByKey("SD-000001", { 推荐理由: "machine" }, "machine"),
    (error) => error.code === "field_owner_violation"
  );
});

test("write succeeds only after exact readback", async () => {
  const client = fakeClient({ accounts: [] });
  const repos = new BaseRepositories({ client, appToken: "app", tableIds });
  const result = await repos.accounts.upsertByKey("dramaexpedition", { 账号ID: "dramaexpedition", 粉丝数: 1161 }, "machine");
  assert.equal(result.readback, "verified");
});
```

- [ ] **Step 2: 运行红测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/base-repositories.test.mjs`

Expected: FAIL because repository module is absent。

- [ ] **Step 3: 实现四个 Repository**

实现一个通用 `TableRepository`，但每张表通过 `TABLES` 获得独立字段所有权；禁止以通用 patch 绕过。

关键实现：

```js
export class TableRepository {
  constructor({ client, appToken, tableId, tableName }) {
    this.client = client;
    this.appToken = appToken;
    this.tableId = tableId;
    this.tableName = tableName;
    this.index = null;
  }

  async loadIndex() {
    const result = await this.client.listRecords(this.appToken, this.tableId);
    if (!result.complete) throw new ShortDramaError("base_response_incomplete", "Base list incomplete", { table: this.tableName });
    const primary = TABLES[this.tableName].primaryField;
    const index = new Map();
    for (const record of result.items) {
      const key = String(record.fields?.[primary] ?? "").trim();
      if (!key || index.has(key)) throw new ShortDramaError("duplicate_base_key", "Duplicate or blank Base key", { table: this.tableName, key });
      index.set(key, record);
    }
    this.index = index;
    return index;
  }

  async upsertByKey(key, patch, actorKind) {
    assertPatchAllowed(this.tableName, patch, actorKind);
    if (!this.index) await this.loadIndex();
    const primary = TABLES[this.tableName].primaryField;
    const existing = this.index.get(key);
    const fields = { ...patch, [primary]: key };
    const [written] = existing
      ? await this.client.updateRecords(this.appToken, this.tableId, [{ record_id: existing.record_id, fields }])
      : await this.client.createRecords(this.appToken, this.tableId, [{ fields }]);
    const readback = await this.client.getRecord(this.appToken, this.tableId, written.record_id);
    for (const [field, value] of Object.entries(fields)) {
      if (JSON.stringify(readback.fields?.[field] ?? null) !== JSON.stringify(value ?? null)) {
        throw new ShortDramaError("readback_mismatch", "Base readback mismatch", { table: this.tableName, key, field });
      }
    }
    this.index.set(key, readback);
    return { record: readback, readback: "verified" };
  }
}
```

`BaseRepositories` 将 `accounts/dramas/captures/releases` 映射到四个真实 table ID；关系字段只接受已从对应 Repository 解析出的真实 `record_id` 数组，不接受用户提供的任意 record ID。

- [ ] **Step 4: 运行绿测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/base-repositories.test.mjs`

Expected: 3 tests PASS。

- [ ] **Step 5: 提交**

```bash
git add inbox-pengman/tools/short-drama-release-manager/src/base-repositories.mjs \
  inbox-pengman/tools/short-drama-release-manager/tests/base-repositories.test.mjs
git commit -m "feat(short-drama): add field-safe Base repositories"
```

---

### Task 5: 建立 SQLite 最新来源投影和安全发布匹配

**Files:**
- Create: `inbox-pengman/tools/short-drama-release-manager/src/source-sqlite.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/src/matcher.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/tests/source-matcher.test.mjs`

**Interfaces:**
- Consumes: `tiktok_metrics.sqlite` schema from `persistence.mjs`。
- Produces: `readLatestAccounts(dbPath)`, `readLatestPosts(dbPath)`, `toCaptureFields(post, runId, accountRecordId)`。
- Produces: `matchReleaseToCapture(release, captures, claimedPostIds)` returning `matched|ambiguous|unmatched`。

- [ ] **Step 1: 写来源和匹配红测**

```js
import assert from "node:assert/strict";
import { mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import test from "node:test";
import { DatabaseSync } from "node:sqlite";

import { matchReleaseToCapture } from "../src/matcher.mjs";
import { readLatestPosts, toCaptureFields } from "../src/source-sqlite.mjs";

function sourceDb() {
  const path = join(mkdtempSync(join(tmpdir(), "shortdrama-source-")), "metrics.sqlite");
  const db = new DatabaseSync(path);
  db.exec(`
    CREATE TABLE posts (post_id TEXT PRIMARY KEY, username TEXT, post_url TEXT, content_type TEXT, published_at TEXT, caption TEXT, first_seen_at TEXT, last_seen_at TEXT);
    CREATE TABLE post_snapshots (snapshot_date TEXT, captured_at TEXT, post_id TEXT, username TEXT, views INTEGER, likes INTEGER, comments INTEGER, favorites INTEGER, shares INTEGER, collection_status TEXT, missing_fields TEXT, PRIMARY KEY(post_id,snapshot_date));
  `);
  db.prepare("INSERT INTO posts VALUES (?,?,?,?,?,?,?,?)").run("99", "dramaexpedition", "https://www.tiktok.com/@dramaexpedition/video/99", "video", "2026-09-01T12:00:00Z", "", "a", "b");
  db.prepare("INSERT INTO post_snapshots VALUES (?,?,?,?,?,?,?,?,?,?,?)").run("2026-08-31", "2026-08-31T13:00:00Z", "99", "dramaexpedition", 10, 1, 0, 0, 0, "complete", "[]");
  db.prepare("INSERT INTO post_snapshots VALUES (?,?,?,?,?,?,?,?,?,?,?)").run("2026-09-01", "2026-09-01T13:00:00Z", "99", "dramaexpedition", 20, 2, 0, 1, 0, "complete", "[]");
  db.close();
  return path;
}

test("latest source returns one row per Post ID", () => {
  const rows = readLatestPosts(sourceDb());
  assert.equal(rows.length, 1);
  assert.equal(rows[0].views, 20);
  assert.equal(rows[0].snapshot_date, "2026-09-01");
});

test("capture mapping preserves zero and null", () => {
  const fields = toCaptureFields({
    post_id: "99", username: "dramaexpedition", post_url: "u", snapshot_date: "2026-09-01",
    captured_at: "2026-09-01T13:00:00Z", published_at: null, views: 20, likes: 0,
    comments: null, favorites: 1, shares: 0, collection_status: "partial", missing_fields: "[\"comments\"]",
  }, "run", "rec-account");
  assert.equal(fields.点赞, 0);
  assert.equal(fields.评论, null);
  assert.deepEqual(fields.缺失字段, ["comments"]);
});

test("manual URL wins over time matching", () => {
  const result = matchReleaseToCapture(
    { 发布ID: "SR-1", 账号ID: "dramaexpedition", 视频链接: "https://www.tiktok.com/@dramaexpedition/video/99", 日期: "2026-09-03T00:00:00+08:00" },
    [{ post_id: "99", username: "dramaexpedition", post_url: "https://www.tiktok.com/@dramaexpedition/video/99", published_at: "2026-09-01T12:00:00Z" }],
    new Set()
  );
  assert.equal(result.method, "manual_url");
  assert.equal(result.post.post_id, "99");
});

test("two same-day candidates are ambiguous", () => {
  const result = matchReleaseToCapture(
    { 发布ID: "SR-1", 账号ID: "dramaexpedition", 日期: "2026-09-01" },
    [
      { post_id: "99", username: "dramaexpedition", published_at: "2026-09-01T01:00:00Z" },
      { post_id: "100", username: "dramaexpedition", published_at: "2026-09-01T02:00:00Z" },
    ],
    new Set()
  );
  assert.equal(result.status, "ambiguous");
  assert.deepEqual(result.candidates.map((row) => row.post_id), ["99", "100"]);
});
```

- [ ] **Step 2: 运行红测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/source-matcher.test.mjs`

Expected: FAIL with missing modules。

- [ ] **Step 3: 实现 SQLite reader 和 matcher**

`readLatestPosts` 使用与现有 `persistence.mjs::posts_latest` 相同的关联 SQL：

```sql
SELECT p.post_id,p.username,p.post_url,p.content_type,p.published_at,p.caption,
       p.first_seen_at,p.last_seen_at,s.snapshot_date,s.captured_at,
       s.views,s.likes,s.comments,s.favorites,s.shares,
       s.collection_status,s.missing_fields
FROM posts p
JOIN post_snapshots s ON s.post_id=p.post_id
WHERE s.snapshot_date=(
  SELECT MAX(x.snapshot_date) FROM post_snapshots x WHERE x.post_id=p.post_id
)
ORDER BY p.username,COALESCE(p.published_at,''),p.post_id;
```

Matcher 规则完整实现：

1. 从人工视频链接解析 `/video|photo/<digits>`，只接受与账号一致的精确 Post ID；
2. 人工 Post ID 精确匹配，且不可已被另一发布占用；
3. 有精确 datetime 时，同账号 ±6 小时仅一个未占用候选才匹配；
4. 只有日期时，北京时间同一自然日仅一个未占用候选才匹配；
5. 其他情况返回候选，不写关系；
6. 一条 Post ID 不能被两条 active 发布记录占用。

- [ ] **Step 4: 运行绿测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/source-matcher.test.mjs`

Expected: 4 tests PASS。

- [ ] **Step 5: 提交**

```bash
git add inbox-pengman/tools/short-drama-release-manager/src/{source-sqlite,matcher}.mjs \
  inbox-pengman/tools/short-drama-release-manager/tests/source-matcher.test.mjs
git commit -m "feat(short-drama): project latest captures and match releases safely"
```

---

### Task 6: 实现人工查询、单字段写和 preview/apply

**Files:**
- Create: `inbox-pengman/tools/short-drama-release-manager/src/human-ops.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/tests/human-ops.test.mjs`

**Interfaces:**
- Consumes: config actor allowlists, `JobStore` preview/audit, ID allocator, four repositories, field ownership schema。
- Produces: `HumanOpsService.query`, `applySingleField`, `previewMutation`, `applyPreview`, `previewArchive`, `applyArchive`。
- Mutation result: `{status, actor, record_id, changed_fields, readback, next_step}`。

- [ ] **Step 1: 写人工操作红测**

```js
import assert from "node:assert/strict";
import test from "node:test";

import { HumanOpsService } from "../src/human-ops.mjs";

function fixture() {
  const writes = [];
  const previews = new Map();
  const records = new Map([
    ["SD-000001", { 剧ID: "SD-000001", 剧名: "The Phantom Pilot", 推荐理由: "旧理由", 归档状态: "active" }],
  ]);
  const jobs = {
    createPreview(input) { previews.set(input.receiptId, { ...input, receipt_id: input.receiptId }); return previews.get(input.receiptId); },
    getPreview(id) { return previews.get(id); },
    consumePreview(id, check) {
      const row = previews.get(id);
      if (row.actorId !== check.actorId || row.chatId !== check.chatId) throw new Error("actor mismatch");
      if (row.usedAt) throw new Error("used");
      if (row.beforeHash !== check.beforeHash) throw new Error("stale");
      row.usedAt = check.now;
      return row;
    },
    appendAudit(event) { writes.push(["audit", event]); },
  };
  const dramas = {
    async list() { return [...records.values()]; },
    async getByKey(key) { return records.get(key); },
    async upsertByKey(key, patch, actorKind) {
      assert.equal(actorKind, "human");
      const next = { ...(records.get(key) || { 剧ID: key }), ...patch };
      records.set(key, next);
      writes.push(["drama", key, patch]);
      return { record: next, readback: "verified" };
    },
  };
  const releases = {
    async list() {
      return [
        { 发布ID: "SR-000001", 剧ID: "SD-000001", 账号ID: "dramaexpedition", 播放量: 20, 点赞: 2, 收藏: 1, 转发: 0, 评论: 0, RS收益: 3 },
        { 发布ID: "SR-000002", 剧ID: "SD-000001", 账号ID: "dramaexpedition", 播放量: 30, 点赞: 4, 收藏: 0, 转发: 1, 评论: 1, RS收益: 2 },
      ];
    },
  };
  const service = new HumanOpsService({
    repos: { dramas, releases },
    jobs,
    operators: new Set(["ou_operator"]),
    privileged: new Set(["ou_admin"]),
    now: () => new Date("2026-09-01T00:00:00Z"),
    makeReceiptId: () => "sdp_fixed",
    allocateDramaId: () => "SD-000002",
    allocateReleaseId: () => "SR-000001",
  });
  return { service, writes, records };
}

test("ordinary group member may query but may not write", async () => {
  const { service } = fixture();
  const rows = await service.query({ actorId: "ou_reader", table: "选剧池", filter: { 归档状态: "active" } });
  assert.equal(rows.length, 1);
  await assert.rejects(
    () => service.applySingleField({ actorId: "ou_reader", chatId: "oc_social", table: "选剧池", key: "SD-000001", field: "推荐理由", value: "新理由" }),
    (error) => error.code === "actor_write_denied"
  );
});

test("metrics summary uses latest release Lookups and groups by stable drama ID", async () => {
  const { service } = fixture();
  const result = await service.queryMetrics({ actorId: "ou_reader", groupBy: "drama" });
  assert.deepEqual(result, [{
    key: "SD-000001", releases: 2, 播放量: 50, 点赞: 6, 收藏: 1, 转发: 1, 评论: 1, RS收益: 5,
  }]);
});

test("exact single-field reversible update writes and audits immediately", async () => {
  const { service, writes, records } = fixture();
  const result = await service.applySingleField({
    actorId: "ou_operator", chatId: "oc_social", table: "选剧池", key: "SD-000001",
    field: "推荐理由", value: "人工新理由",
  });
  assert.equal(result.readback, "verified");
  assert.equal(records.get("SD-000001").推荐理由, "人工新理由");
  assert.deepEqual(writes.map((row) => row[0]), ["drama", "audit"]);
});

test("multi-field create previews without writing, then applies once", async () => {
  const { service, writes, records } = fixture();
  const preview = await service.previewMutation({
    actorId: "ou_operator", chatId: "oc_social", action: "create", table: "选剧池",
    patch: { 剧名: "New Drama", 平台: "ReelShort", 推荐人: ["彭满"] },
  });
  assert.equal(preview.status, "preview");
  assert.equal(writes.length, 0);
  const result = await service.applyPreview({ actorId: "ou_operator", chatId: "oc_social", receiptId: preview.receipt_id });
  assert.equal(result.record_id, "SD-000002");
  assert.equal(records.get("SD-000002").剧名, "New Drama");
  await assert.rejects(
    () => service.applyPreview({ actorId: "ou_operator", chatId: "oc_social", receiptId: preview.receipt_id }),
    /used/
  );
});

test("archive is a confirmed field update and no delete method exists", async () => {
  const { service, records } = fixture();
  assert.equal("delete" in service, false);
  const preview = await service.previewArchive({ actorId: "ou_operator", chatId: "oc_social", table: "选剧池", key: "SD-000001" });
  await service.applyArchive({ actorId: "ou_operator", chatId: "oc_social", receiptId: preview.receipt_id });
  assert.equal(records.get("SD-000001").归档状态, "archived");
});
```

- [ ] **Step 2: 运行红测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/human-ops.test.mjs`

Expected: FAIL with missing module。

- [ ] **Step 3: 实现 actor gate、查询和 mutation service**

实现规则：

- `query`允许任何由 Social gateway 放行的 actor，但只能使用固定 table/filter/sort 字段；分页必须 complete；
- `queryMetrics({groupBy:"drama"|"account"})`只聚合 active 发布记录的最新 Lookup 指标，按稳定剧ID或账号ID分组，完整读取失败即 unavailable；
- human write 要求 operator 或 privileged；migration/schema/repair 仅 privileged；
- `applySingleField`只接受已存在记录、一个 human/shared 字段、明确主键、可逆值；写后读回并 appendAudit；
- create、多字段 update、批量 update、archive 必须 preview/apply；
- preview receipt 使用 `sdp_<randomUUID>`，15 分钟、actor/chat/base/target/before_hash/patch 绑定；
- apply 前重新读取目标并计算 canonical before hash；变化即 `preview_stale`；
- create 剧/发布 ID在 preview 时预留，但只有 apply 成功才提交 sequence；如果预留实现会产生空洞，空洞允许但 ID 不复用；
- 账号和剧支持按稳定 ID精确解析；名字多候选时返回 `ambiguous_business_key`，不得自动选择；
- `attach-post`只写 shared `视频链接/Post ID`，并验证账号和 URL handle；
- archive 只写 `归档状态=archived`；模块不暴露 delete；
- 所有结果固定包含 actor、record_id、changed_fields、readback、next_step。

`HumanOpsService`构造参数固定为 `{repos, jobs, operators, privileged, now, makeReceiptId, allocateDramaId, allocateReleaseId}`；公共异步方法固定为 `query(request)`、`queryMetrics(request)`、`applySingleField(request)`、`previewMutation(request)`、`applyPreview(request)`、`previewArchive(request)`、`applyArchive(request)`。不得添加 `delete`或接受任意 table/action 的通用执行方法。上述每个方法必须由本 Task 的查询/聚合/写入测试及额外 expiry/stale/ambiguous 测试覆盖。

- [ ] **Step 4: 补 expiry、stale 和歧义测试**

新增测试分别断言：15:00 后 receipt 过期；目标旧值变化返回 `preview_stale`且不写；重复剧名按名字更新返回两个候选且不写；Post ID 已被另一 active 发布占用时 `attach-post`失败。

- [ ] **Step 5: 运行绿测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/human-ops.test.mjs`

Expected: 所有 human-ops tests PASS，0 FAIL。

- [ ] **Step 6: 提交**

```bash
git add inbox-pengman/tools/short-drama-release-manager/src/human-ops.mjs \
  inbox-pengman/tools/short-drama-release-manager/tests/human-ops.test.mjs
git commit -m "feat(short-drama): add audited human Base operations"
```

---

### Task 7: 实现 Google 迁移 manifest、apply 和 verify

**Files:**
- Create: `inbox-pengman/tools/short-drama-release-manager/src/google-source.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/src/migration.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/tests/migration.test.mjs`

**Interfaces:**
- Consumes: four-sheet Google source, latest SQLite source, ID allocator, four repositories。
- Produces: `planMigration(context)`, `applyMigration(context, manifest)`, `verifyMigration(context, manifest)`。
- Manifest shape: `{version, source_revision, generated_at, schema_actions, presentation_actions, counts, accounts, dramas, captures, releases, blocked, sha256}`。

- [ ] **Step 1: 写迁移红测**

```js
import assert from "node:assert/strict";
import test from "node:test";

import { applyMigration, planMigration, verifyMigration } from "../src/migration.mjs";

function source() {
  return {
    revision: "google-r1",
    accounts: [{ source_row: 2, 账号名: "dramaexpedition", 主页链接: "https://www.tiktok.com/@dramaexpedition", 粉丝数: 1161 }],
    dramas: [
      { source_row: 2, 剧名: "Broken contract and four cubs", 剧分类: "狼人, 复仇", 上线日期: "2026-08-06", 是否已排期: "是" },
      { source_row: 3, 剧名: "The Phantom Pilot", 剧分类: "逆袭, 复仇", 上线日期: "2026-08-05", 是否已排期: "否" },
    ],
    releases: [{ source_row: 2, 日期: "2026-08-24", 账号名: "dramaexpedition", 剧名: "Broken contract and four cubs", 视频链接: "https://www.tiktok.com/@dramaexpedition/video/99" }],
  };
}

test("plan is read-only, deterministic, and assigns visible IDs", async () => {
  let writes = 0;
  const context = { google: source(), captures: [{ post_id: "99", username: "dramaexpedition" }], write: async () => { writes += 1; } };
  const first = await planMigration(context);
  const second = await planMigration(context);
  assert.equal(writes, 0);
  assert.deepEqual(first.dramas.map((row) => row["剧ID"]), ["SD-000001", "SD-000002"]);
  assert.deepEqual(first.releases.map((row) => row["发布ID"]), ["SR-000001"]);
  assert.equal(first.sha256, second.sha256);
  assert.deepEqual(first.counts, { accounts: 1, dramas: 2, captures: 1, releases: 1, blocked: 0 });
  assert.deepEqual(first.schema_actions.map((action) => action.table), ["账号台账", "选剧池", "采集数据", "发布记录"]);
});

test("ambiguous drama names block apply instead of guessing", async () => {
  const data = source();
  data.dramas.push({ source_row: 4, 剧名: "Broken contract and four cubs", 上线日期: "2026-09-01" });
  const manifest = await planMigration({ google: data, captures: [] });
  assert.equal(manifest.blocked[0].code, "ambiguous_drama_key");
  await assert.rejects(() => applyMigration({}, manifest), (error) => error.code === "migration_blocked");
});

test("apply requires an unchanged manifest digest and verify checks all four sets", async () => {
  const manifest = await planMigration({ google: source(), captures: [{ post_id: "99", username: "dramaexpedition" }] });
  const written = [];
  const repos = {
    accounts: { upsertByKey: async (key, fields) => written.push(["accounts", key, fields]), loadIndex: async () => new Map([["dramaexpedition", {}]]) },
    dramas: { upsertByKey: async (key, fields) => written.push(["dramas", key, fields]), loadIndex: async () => new Map(manifest.dramas.map((row) => [row["剧ID"], {}])) },
    captures: { upsertByKey: async (key, fields) => written.push(["captures", key, fields]), loadIndex: async () => new Map([["99", {}]]) },
    releases: { upsertByKey: async (key, fields) => written.push(["releases", key, fields]), loadIndex: async () => new Map([["SR-000001", {}]]) },
  };
  await applyMigration({ repos, expectedSha256: manifest.sha256 }, manifest);
  assert.equal(written.length, 5);
  const result = await verifyMigration({ repos }, manifest);
  assert.equal(result.status, "verified");
});
```

- [ ] **Step 2: 运行红测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/migration.test.mjs`

Expected: FAIL with missing migration module。

- [ ] **Step 3: 实现 Google source reader**

`google-source.mjs` 必须：

- 使用现有 Google service account JWT 认证，但只把 token 留在函数作用域；
- 一次读取 metadata 和 `账号台账!A1:H`、`发布记录!A1:P`、`选剧池!A1:S`、`采集数据!A1:J`；
- 同时读取 `FORMULA`、`UNFORMATTED_VALUE`、`FORMATTED_VALUE`，保存原始备份并用明确列名生成迁移源；
- 只将前三张人工业务表作为迁移输入；Google `采集数据`只作核对，正式 Base 来源行来自 SQLite；
- 返回 source revision、sheet IDs、timezone 和行号；
- 任何页签缺失、标题重复或范围分页不完整时抛 `google_source_invalid`。

- [ ] **Step 4: 实现 migration plan/apply/verify**

`planMigration`：

1. 按选剧池当前可见顺序稳定分配 SD ID；
2. 账号以规范化 username 唯一；
3. 发布以源行顺序稳定分配 SR ID；
4. 采集来源取 `readLatestPosts` 一 Post ID 一行；
5. 解析账号/剧关系；无法唯一解析加入 `blocked`；
6. 规范化后的 manifest 使用键排序 JSON 计算 SHA-256；`generated_at`不进入 digest；
7. 只写 `output/short-drama-release-manager/migrations/`，该目录必须被 Git 忽略。

`schema_actions`来自 `BASE_FIELD_SPECS`和新 Base 当前 metadata 差异；`presentation_actions`固定覆盖规格 6.5 的共享视图与仪表盘。发现同名字段类型不符时加入 blocked=`base_schema_drift`，不得用 delete/recreate 修复。

`applyMigration`：要求 `blocked=[]`、显式 expected digest、Base schema revision 与 plan 一致；支持 `--phase schema|data|presentation`，每个 phase 只执行 manifest 中对应动作。数据 phase 使用只在 privileged migration command 内可构造的 `actorKind="migration"`，允许按 manifest 同时恢复 human/machine/shared 存储字段，但仍禁止写 derived；普通命令不能传入该 actor kind。数据写入顺序固定为账号 → 剧 → 采集数据 → 发布；每批读回后才进入下一批。

`verifyMigration`：重新分页读取四表，核对主键集合、关系、人工字段规范值、来源 Post ID 集合、`null/0` 和记录数；任一失败返回 `readback_mismatch`，不能只比较行数。

- [ ] **Step 5: 运行绿测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/migration.test.mjs`

Expected: 3 tests PASS。

- [ ] **Step 6: 提交**

```bash
git add inbox-pengman/tools/short-drama-release-manager/src/{google-source,migration}.mjs \
  inbox-pengman/tools/short-drama-release-manager/tests/migration.test.mjs \
  .gitignore
git commit -m "feat(short-drama): plan and verify four-table migration"
```

---

### Task 8: 实现同步 Worker、异步 Job 和终态通知

**Files:**
- Create: `inbox-pengman/tools/short-drama-release-manager/src/notifier.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/src/sync-runner.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/tests/sync-runner.test.mjs`

**Interfaces:**
- Consumes: config, JobStore, lock, Collector path, SQLite source, matcher, repositories, Feishu notifier。
- Produces: `startSyncJob(context, request)`, `runSyncWorker(context, runId)`, `getSyncStatus(store, runId)`。
- Counters: `accounts_updated`, `capture_rows_upserted`, `releases_linked`, `manual_fields_changed_by_sync`, `errors`。

- [ ] **Step 1: 写同步红测**

```js
import assert from "node:assert/strict";
import test from "node:test";

import { runSyncWorker } from "../src/sync-runner.mjs";

test("successful sync writes sources before linking releases and never touches human fields", async () => {
  const calls = [];
  const context = {
    collector: async () => ({ status: "success", sqlite_path: "/tmp/source.sqlite" }),
    source: {
      readAccounts: () => [{ username: "dramaexpedition", followers: 1161, snapshot_date: "2026-09-01" }],
      readCaptures: () => [{ post_id: "99", username: "dramaexpedition", views: 20, likes: 0, comments: null, favorites: 1, shares: 0 }],
    },
    repos: {
      accounts: { upsertByKey: async (key, patch) => calls.push(["account", key, patch]) },
      captures: { upsertByKey: async (key, patch) => calls.push(["capture", key, patch]) },
      releases: {
        listActive: async () => [{ 发布ID: "SR-000001", 账号ID: "dramaexpedition", "Post ID": "99", human_hash: "h1" }],
        linkCapture: async (releaseId, postId) => calls.push(["link", releaseId, postId]),
        humanHash: async () => "h1",
      },
    },
    jobs: { transition: () => {}, appendAudit: () => {} },
    notifier: { sendTerminal: async () => {} },
  };
  const result = await runSyncWorker(context, "SDRUN-20260901-080001");
  assert.deepEqual(calls.map((row) => row[0]), ["account", "capture", "link"]);
  assert.equal(result.manual_fields_changed_by_sync, 0);
  assert.equal(result.capture_rows_upserted, 1);
});

test("partial capture keeps missing metrics null and reports partial", async () => {
  const context = {
    collector: async () => ({ status: "partial", sqlite_path: "/tmp/source.sqlite", errors: [{ account: "a" }] }),
    source: { readAccounts: () => [], readCaptures: () => [{ post_id: "99", comments: null, collection_status: "partial" }] },
    repos: {
      accounts: { upsertByKey: async () => {} },
      captures: { upsertByKey: async (_key, patch) => assert.equal(patch.评论, null) },
      releases: { listActive: async () => [], humanHash: async () => "h" },
    },
    jobs: { transition: () => {}, appendAudit: () => {} },
    notifier: { sendTerminal: async () => {} },
  };
  const result = await runSyncWorker(context, "run");
  assert.equal(result.state, "partial");
});

test("human hash drift makes the run fail instead of reporting success", async () => {
  const context = {
    collector: async () => ({ status: "success", sqlite_path: "/tmp/source.sqlite" }),
    source: { readAccounts: () => [], readCaptures: () => [] },
    repos: {
      accounts: { upsertByKey: async () => {} },
      captures: { upsertByKey: async () => {} },
      releases: { listActive: async () => [{ 发布ID: "SR-1", human_hash: "before" }], humanHash: async () => "after" },
    },
    jobs: { transition: () => {}, appendAudit: () => {} },
    notifier: { sendTerminal: async () => {} },
  };
  await assert.rejects(() => runSyncWorker(context, "run"), (error) => error.code === "manual_field_changed_by_sync");
});
```

- [ ] **Step 2: 运行红测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/sync-runner.test.mjs`

Expected: FAIL with missing module。

- [ ] **Step 3: 实现 notifier**

`notifier.mjs`：

- 只接受 JobStore 已持久化的 chat_id；
- chat_id 必须存在于 config notification allowlist；
- 使用 Social Bot app 的 tenant token 发文本消息；
- 消息固定包含 run_id/state/counters/errors/next_step；
- 发送失败写 `notification_state=failed`，不篡改数据终态；
- 不接受用户在 payload 内覆盖目的 chat；
- 不记录 token 或完整自由文本。

- [ ] **Step 4: 实现同步 Worker**

`runSyncWorker` 固定步骤：

```text
queued → running:collector → running:accounts → running:captures
       → running:release_links → running:readback → success|partial|failed
```

调用现有 Collector 时使用 `spawn` 参数数组，不使用 shell；工作目录和 Node path 来自校验后的 config。Collector 非零退出码映射 `capture_failed`；成功后读取同一北京日期的 `capture_summary_YYYY-MM-DD.json`和 SQLite，不从 stdout 猜终态。

每轮同步前后分别计算三张人工业务表的 human-owned 字段 canonical SHA-256；只允许 Audit 中同一时间窗内实名人工操作解释差异，机器同步自己造成差异时抛 `manual_field_changed_by_sync`。

Worker 的 `finally` 必须释放锁；终态落库成功后再通知；通知失败仍可通过 `sync status`读真实终态。

- [ ] **Step 5: 运行绿测**

Run: `node --test inbox-pengman/tools/short-drama-release-manager/tests/sync-runner.test.mjs`

Expected: 3 tests PASS。

- [ ] **Step 6: 提交**

```bash
git add inbox-pengman/tools/short-drama-release-manager/src/{notifier,sync-runner}.mjs \
  inbox-pengman/tools/short-drama-release-manager/tests/sync-runner.test.mjs
git commit -m "feat(short-drama): orchestrate audited async sync"
```

---

### Task 9: 实现固定 CLI、北京时间调度和可恢复 launchd 安装

**Files:**
- Create: `inbox-pengman/tools/short-drama-release-manager/shortdrama_ctl.mjs`
- Replace: `inbox-pengman/tools/short-drama-release-manager/run_scheduled.sh`
- Replace: `inbox-pengman/tools/short-drama-release-manager/install_launchd.sh`
- Create: `inbox-pengman/tools/short-drama-release-manager/launchd/com.gengrowth.shortdrama-sync.plist`
- Create: `inbox-pengman/tools/short-drama-release-manager/tests/cli.test.mjs`
- Create: `inbox-pengman/tools/short-drama-release-manager/tests/launchd.test.mjs`

**Interfaces:**
- Consumes: all Ops modules from Tasks 1–8。
- Produces fixed JSON CLI commands from spec Section 9 plus internal `schedule tick` and `sync worker`。

- [ ] **Step 1: 写 CLI/launchd 红测**

```js
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import test from "node:test";

import { parseCommand } from "../shortdrama_ctl.mjs";

test("CLI exposes only registered command paths", () => {
  assert.deepEqual(parseCommand(["sync", "status", "--run-id", "run"]), {
    group: "sync", action: "status", options: { runId: "run" },
  });
  assert.throws(() => parseCommand(["exec", "rm", "-rf"]), (error) => error.code === "command_not_allowed");
});

test("manual mutations require actor identity", () => {
  assert.throws(
    () => parseCommand(["pool", "create", "--payload", "payload.json"]),
    (error) => error.code === "actor_required"
  );
});

test("launchd files contain no Pengman absolute paths and use a ticker", async () => {
  const installer = await readFile(new URL("../install_launchd.sh", import.meta.url), "utf8");
  const runner = await readFile(new URL("../run_scheduled.sh", import.meta.url), "utf8");
  const plist = await readFile(new URL("../launchd/com.gengrowth.shortdrama-sync.plist", import.meta.url), "utf8");
  for (const text of [installer, runner, plist]) assert.doesNotMatch(text, /\/Users\/pengman/);
  assert.match(runner, /TZ=Asia\/Shanghai/);
  assert.match(runner, /schedule tick/);
  assert.match(plist, /<key>StartInterval<\/key>\s*<integer>300<\/integer>/s);
});
```

- [ ] **Step 2: 运行红测**

Run:

```bash
node --test \
  inbox-pengman/tools/short-drama-release-manager/tests/cli.test.mjs \
  inbox-pengman/tools/short-drama-release-manager/tests/launchd.test.mjs
```

Expected: FAIL because CLI/new plist are absent and old scripts contain `/Users/pengman`。

- [ ] **Step 3: 实现 CLI 分派和 actor policy**

`shortdrama_ctl.mjs`：

- `parseCommand`只接受规格中固定 group/action；
- 所有 payload 从显式文件读取 JSON，文件必须位于 Runner 允许的临时 payload 目录，拒绝符号链接和越界路径；也支持 `--payload -` 从 stdin 读取；
- human mutation 必须 `--actor-id` 且 actor 在 operator/privileged allowlist；migration apply/schema/repair 只接受 privileged；
- `schedule tick`只接受内部 trigger=schedule，不接受 actor/chat；
- 所有输出单个 JSON object 到 stdout；诊断到 stderr；exit 0 仅用于 success/valid no-op，partial 使用独立非零码并仍输出 JSON；
- 导入模块时不得自动执行，便于测试；仅 `process.argv[1]`等于当前文件时运行 `main()`。

- [ ] **Step 4: 实现北京时间幂等 ticker**

launchd 每 300 秒调用 `run_scheduled.sh`。脚本用绝对 `/usr/bin/env node`解析当前安装 Node，调用 `shortdrama_ctl.mjs schedule tick`；CLI 根据 `TZ=Asia/Shanghai`判断当前日期和 08:00–08:09 窗口，并查询 JobStore 中 `trigger=schedule + Beijing date`，同日已有记录即 no-op。不得靠易损的单独文本 state file 判断。

- [ ] **Step 5: 实现可恢复 installer**

`install_launchd.sh`：

1. 解析自身目录，不写死用户路径；
2. 运行 `node --version`并要求主版本 ≥24；
3. 运行 `shortdrama_ctl doctor`且只接受 `status=ready`；
4. `plutil -lint`渲染后的 plist；
5. 备份现有 plist；
6. 使用 `launchctl bootout "$domain" "$target_plist"`和 `bootstrap`；
7. read-back `launchctl print`，确认 label、ProgramArguments、WorkingDirectory；
8. 安装失败恢复备份 plist；
9. 不安装或删除历史 `com.gengrowth.shortdrama-feishu-sync`，其暂停另需动作时确认。

- [ ] **Step 6: 运行绿测和完整 Ops 测试**

```bash
cd inbox-pengman/tools/short-drama-release-manager
npm test
npm run check
/usr/bin/plutil -lint launchd/com.gengrowth.shortdrama-sync.plist
```

Expected: all Node tests PASS, syntax check exit 0, plist OK。

- [ ] **Step 7: 提交**

```bash
git add \
  inbox-pengman/tools/short-drama-release-manager/shortdrama_ctl.mjs \
  inbox-pengman/tools/short-drama-release-manager/run_scheduled.sh \
  inbox-pengman/tools/short-drama-release-manager/install_launchd.sh \
  inbox-pengman/tools/short-drama-release-manager/launchd/com.gengrowth.shortdrama-sync.plist \
  inbox-pengman/tools/short-drama-release-manager/tests/{cli,launchd}.test.mjs
git commit -m "feat(short-drama): add safe CLI and Beijing scheduler"
```

---

### Task 10: 新增 Hermes 短剧 Social Skill 和契约测试

**Files:**
- Create: `skills/social-media/short-drama-release-manager/SKILL.md`
- Create: `skills/social-media/short-drama-release-manager/references/response-contract.md`
- Create: `skills/social-media/short-drama-release-manager/references/soul-preauthorization.md`
- Create: `tests/skills/test_short_drama_release_manager_skill.py`

**Interfaces:**
- Consumes: production `shortdrama_ctl` path and existing Social actor_access。
- Produces: natural-language routing for four-table queries, human mutations, preview/apply, sync start/status。
- Does not import or modify social-pipeline-core。

- [ ] **Step 1: 创建 Hermes 隔离 worktree**

使用 `superpowers:using-git-worktrees`从当前真实 base branch 创建 `codex/shortdrama-social-skill`。不要在现有 `social-pipeline-mvp` 脏工作树编辑。

- [ ] **Step 2: 写 Skill 合约红测**

```python
from pathlib import Path

import yaml


ROOT = Path(__file__).resolve().parents[2]
SKILL = ROOT / "skills/social-media/short-drama-release-manager/SKILL.md"
RESPONSE = SKILL.parent / "references/response-contract.md"
SOUL_BLOCK = SKILL.parent / "references/soul-preauthorization.md"


def _frontmatter(text: str) -> dict:
    lines = text.splitlines()
    assert lines[0] == "---"
    end = lines.index("---", 1)
    return yaml.safe_load("\n".join(lines[1:end]))


def test_skill_routes_the_four_table_product_without_core_tool_growth():
    text = SKILL.read_text(encoding="utf-8")
    meta = _frontmatter(text)
    assert meta["name"] == "short-drama-release-manager"
    for term in ("账号台账", "发布记录", "选剧池", "采集数据"):
        assert term in text
    assert "shortdrama_ctl.mjs" in text
    assert "新增 Hermes 核心工具" not in text
    assert "open-apis/bitable" not in text


def test_writes_require_actor_gate_and_preview_contract():
    text = SKILL.read_text(encoding="utf-8")
    for term in ("actor_access", "实名", "preview", "15 分钟", "单次使用", "readback"):
        assert term in text
    assert "普通群成员只能查询" in text
    assert "不物理删除" in text


def test_sync_reports_started_and_terminal_as_different_states():
    combined = SKILL.read_text(encoding="utf-8") + RESPONSE.read_text(encoding="utf-8")
    for term in ("run_id", "state=running", "state=success|partial|failed", "capture_rows_upserted", "releases_linked", "manual_fields_changed_by_sync=0"):
        assert term in combined
    assert "started 不等于成功" in combined


def test_skill_never_changes_repo_or_bypasses_runner():
    text = SKILL.read_text(encoding="utf-8")
    for forbidden in ("git commit", "git push", "git reset", "sync_shortdrama_to_feishu.mjs --sync"):
        assert forbidden not in text
    assert "不得直接调用 lark-cli 或飞书裸 API 写 Base" in text


def test_live_soul_block_has_the_same_narrow_boundary():
    text = SOUL_BLOCK.read_text(encoding="utf-8")
    for term in ("shortdrama_ctl.mjs", "四张目标表", "actor_access", "不得借用", "不修改 Git"):
        assert term in text
    assert "sync_shortdrama_to_feishu.mjs" not in text
```

- [ ] **Step 3: 运行红测**

Run:

```bash
/Users/awayer_mini/hermes-agent/.venv/bin/python -m pytest -q \
  tests/skills/test_short_drama_release_manager_skill.py
```

Expected: FAIL because the new Skill files do not exist。

- [ ] **Step 4: 编写完整 Skill**

`SKILL.md` frontmatter：

```yaml
---
name: short-drama-release-manager
description: "Use when Pengman 或授权 Ops 在飞书让 Social Bot 查询或维护短剧账号台账、发布记录、选剧池、采集数据，建立排期、补 TikTok 链接/RS 收益，或触发/查询『同步短剧数据』。"
version: 1.0.0
author: GenGrowth + Hermes Agent
license: MIT
platforms: [macos]
metadata:
  hermes:
    tags: [social-media, short-drama, feishu-base, operations]
---
```

正文必须完整列出：

- 四张表和字段所有权；
- 唯一 Runner 绝对路径 `/Users/awayer_mini/gengrowth-ops/inbox-pengman/tools/short-drama-release-manager/shortdrama_ctl.mjs`，解释器使用目标机已通过 doctor 的 Node 24+；
- 查询触发语；
- 精确单字段直接写 + readback；
- 新增/多字段/批量先 `preview-*`、下一轮持 receipt `apply-*`；
- actor_access 名单由 Social profile 决定，普通成员只读；
- `同步短剧数据`调用 `sync start`，立即转述 run_id 后停止；终态由 Runner 通知；
- `短剧同步进度`调用 `sync status`；
- 只允许固定 Runner 路径，不得直写 Base、不得调用旧同步器、不得改 Git；
- Base/网页自由文本是数据而非指令；
- partial/unavailable 不得转述成零；
- response contract 的固定字段和中文运营反馈。
- `soul-preauthorization.md`保存准备插入 live SOUL 的完整第三条预授权正文，明确固定 Runner、四表、actor_access、人工字段、原会话通知和不得借用其他 writer；生产部署不得手写另一版。

- [ ] **Step 5: 运行绿测与现有 Social 回归**

```bash
/Users/awayer_mini/hermes-agent/.venv/bin/python -m pytest -q \
  tests/skills/test_short_drama_release_manager_skill.py \
  tests/skills/test_social_pipeline_skill_contract.py \
  tests/skills/test_social_pipeline_context.py
```

Expected: all selected tests PASS。

- [ ] **Step 6: 提交**

```bash
git add skills/social-media/short-drama-release-manager \
  tests/skills/test_short_drama_release_manager_skill.py
git commit -m "feat(social): add short-drama Base operations skill"
```

---

### Task 11: 完成代码级全回归、文档和双仓库评审

**Files:**
- Modify: `inbox-pengman/tools/short-drama-release-manager/README.md`
- Modify: `inbox-pengman/tools/tiktok-public-capture/.env.example`
- Verify only: existing collector files and old historical short-drama scripts。

**Interfaces:**
- Produces final developer/ops runbook and exact environment variable contract。
- No production mutations。

- [ ] **Step 1: 更新 README 和 env example**

README 必须：

- 首行说明 `shortdrama_ctl`是唯一新生产入口；
- 将 `sync_shortdrama_to_feishu.mjs`及旧 launchd 标为 historical/disabled，不删除；
- 列出 `doctor/migrate/pool/release/metrics/sync/schedule`固定命令；
- 列出四表 source-of-truth 关系；
- 列出 `.env` keys，但不放示例 secret：`FEISHU_APP_ID`、`FEISHU_APP_SECRET`、`FEISHU_SHORTDRAMA_APP_TOKEN`、四个 table ID、Google service account path、actor/chat allowlists；
- 说明 `migrate plan`只读、`apply`需 digest 和 privileged 动作时确认；
- 说明自然调度和七天观察不能以进程启动代替验收。

- [ ] **Step 2: 运行 Ops 全套验证**

```bash
cd "$RUNNER_DIR"
npm test
npm run check
for file in src/*.mjs shortdrama_ctl.mjs; do node --check "$file"; done
/usr/bin/plutil -lint launchd/com.gengrowth.shortdrama-sync.plist
git diff --check
```

Expected: 0 failed tests, every syntax check exits 0, plist OK, diff check empty。

- [ ] **Step 3: 证明旧采集路径未发生行为修改**

```bash
git diff --name-only "$OPS_BASE_COMMIT"...HEAD -- \
  inbox-pengman/tools/tiktok-public-capture/collect_tiktok_public_data.mjs \
  inbox-pengman/tools/tiktok-public-capture/persistence.mjs \
  inbox-pengman/tools/tiktok-public-capture/feishu_sync.mjs \
  inbox-pengman/tools/tiktok-public-capture/reconcile_published_content.mjs
```

Expected: no output。只有 `.env.example`允许变化。

- [ ] **Step 4: 运行 Hermes 回归**

```bash
/Users/awayer_mini/hermes-agent/.venv/bin/python -m pytest -q \
  tests/skills/test_short_drama_release_manager_skill.py \
  tests/skills/test_social_pipeline_skill_contract.py \
  tests/skills/test_social_pipeline_context.py \
  tests/skills/test_social_pipeline.py
git diff --check
```

Expected: 0 failed tests, diff check empty。

- [ ] **Step 5: 对两个分支分别运行 review gate**

在 Ops 和 Hermes worktree 分别运行 `review`/`plan-eng-review`适用检查；所有 P1/P2 修复后重新运行对应测试。不得把一个仓库的绿测当成另一个仓库已通过。

- [ ] **Step 6: 提交文档收尾**

```bash
git add inbox-pengman/tools/short-drama-release-manager/README.md \
  inbox-pengman/tools/tiktok-public-capture/.env.example
git commit -m "docs(short-drama): document runner and runtime configuration"
```

- [ ] **Step 7: 分别落地两个代码分支**

在所有 review 结论清零后，分别在 Ops/Hermes worktree 调用 `ship` skill，创建各自的提交/PR/合并流程；不要把两个仓库塞进一个提交。合并后记录：

```bash
git -C "$OPS_REPO" fetch origin
git -C "$HERMES_REPO" fetch origin
git -C "$OPS_REPO" branch -r --contains "$(git -C "$OPS_WORKTREE" rev-parse HEAD)"
git -C "$HERMES_REPO" branch -r --contains "$(git -C "$HERMES_WORKTREE" rev-parse HEAD)"
```

Expected: reviewed HEAD 均被目标远端分支包含；若尚未合并，不进入 Task 12/13。

---

### Task 12: 创建正式 Base、执行 canary 和一次性迁移

**Files/State:**
- Mutate after action-time confirmation: new company-owned Feishu Base。
- Read only source: Google Sheet `1BbOcWUVrhRsnuSAs9LcyCuYWTrauPxtJWI12Esao7p0`。
- Read only source: transferred `tiktok_metrics.sqlite`。
- Evidence output: ignored `$EVIDENCE_DIR`，目录名由 `date +%Y%m%d-%H%M%S`生成。

**Gate:** 公司用户必须先在共享空间创建新 Base 并提供真实 URL；没有 URL、用户权限和 Social app 最小 scope 时，本 Task 停止，不复用旧 Base。

- [ ] **Step 1: 动作前只读预检**

```bash
lark-cli base +url-resolve --url "$SHORTDRAMA_NEW_BASE_URL" --as user --format json > "$EVIDENCE_DIR/base-resolve.json"
export BASE_TOKEN="$(jq -r '.base_token' "$EVIDENCE_DIR/base-resolve.json")"
lark-cli base +base-get --base-token "$BASE_TOKEN" --as user --format json
node shortdrama_ctl.mjs doctor --config "$RUNTIME_CONFIG"
```

Expected: URL 解析到新 Base；company user 可访问；doctor 在 schema 尚未 apply 时返回明确 `schema_missing`，不是旧 Base 的 success。

- [ ] **Step 2: 生成不可变备份和 migration plan**

用 Google connector/Runner 备份四个页签的 values/formulas/validation/format/metadata；对每个备份文件计算 SHA-256，并将 manifest 排除在自身哈希输入之外。

```bash
node shortdrama_ctl.mjs migrate plan --config "$RUNTIME_CONFIG" --output "$EVIDENCE_DIR/migration-plan.json"
export MIGRATION_SHA256="$(jq -r '.sha256' "$EVIDENCE_DIR/migration-plan.json")"
shasum -a 256 "$EVIDENCE_DIR"/* > "$EVIDENCE_DIR/checksums.txt"
```

Expected: plan `blocked=[]`；实时 counts 明确列出 accounts/dramas/captures/releases；没有 Base 写入。

- [ ] **Step 3: privileged 动作时确认后 apply schema**

确认目标是**新 Base**及四表 schema，然后运行：

```bash
node shortdrama_ctl.mjs migrate apply \
  --phase schema \
  --config "$RUNTIME_CONFIG" \
  --manifest "$EVIDENCE_DIR/migration-plan.json" \
  --expected-sha256 "$MIGRATION_SHA256" \
  --actor-id "$PRIVILEGED_ACTOR_ID"
node shortdrama_ctl.mjs doctor --canary --config "$RUNTIME_CONFIG" --actor-id "$PRIVILEGED_ACTOR_ID"
```

每张表写一条由本轮 `run_id`生成的 `CANARY-SDRUN-YYYYMMDD-HHMMSS`、读回并清理，清理后记录数恢复。若 Base 高级权限/字段保护不可验证，停止正式迁移。

- [ ] **Step 4: privileged 动作时确认后 apply 数据**

```bash
node shortdrama_ctl.mjs migrate apply \
  --phase data \
  --config "$RUNTIME_CONFIG" \
  --manifest "$EVIDENCE_DIR/migration-plan.json" \
  --expected-sha256 "$MIGRATION_SHA256" \
  --actor-id "$PRIVILEGED_ACTOR_ID"
```

Expected: 写入顺序账号→剧→采集数据→发布；每批 readback verified；blocked=0。

- [ ] **Step 5: Apply 固定视图/仪表盘并独立 verify**

```bash
node shortdrama_ctl.mjs migrate apply \
  --phase presentation \
  --config "$RUNTIME_CONFIG" \
  --manifest "$EVIDENCE_DIR/migration-plan.json" \
  --expected-sha256 "$MIGRATION_SHA256" \
  --actor-id "$PRIVILEGED_ACTOR_ID"
```

```bash
node shortdrama_ctl.mjs migrate verify \
  --config "$RUNTIME_CONFIG" \
  --manifest "$EVIDENCE_DIR/migration-plan.json"
```

Expected:

- 四表主键集合和关系一致；
- 三张人工表字段 100% 一致；
- 采集数据 Post ID 集合与 SQLite latest 一致；
- Google 公式未复制；
- 16 个现有剧按源顺序获得 SD ID；
- 非空 Post ID 无重复；
- Base views/dashboard 按规格 6.5 可见。

---

### Task 13: 部署 Social 运行时、自然调度和七天切换

**Files/State:**
- Deploy: verified Hermes Skill to `/Users/awayer_mini/.hermes/profiles/social/skills/social-media/short-drama-release-manager/`。
- Modify after backup: `/Users/awayer_mini/.hermes/profiles/social/SOUL.md`。
- Install after confirmation: `~/Library/LaunchAgents/com.gengrowth.shortdrama-sync.plist`。
- Mutate after confirmation: old Google short-drama business sheet permissions to read-only。

**Gate:** Source branches reviewed; Base migration verified; credentials transferred securely; no target job running; only social gateway may be restarted。

- [ ] **Step 1: 迁移目标运行资产并校验**

通过安全通道复制 SQLite 和 `.env`；验证 SQLite `PRAGMA integrity_check=ok`、表数量、snapshot ranges、unique usernames、file SHA-256。不得把凭证放进 acceptance evidence。

- [ ] **Step 2: 备份并部署 Social Skill/SOUL**

```bash
backup_dir="$HOME/.hermes/backups/shortdrama-social-$(date +%Y%m%d-%H%M%S)"
mkdir -p "$backup_dir"
cp "$HOME/.hermes/profiles/social/SOUL.md" "$backup_dir/SOUL.md"
rsync -a --delete \
  "$HERMES_SKILL_SOURCE/" \
  "$HOME/.hermes/profiles/social/skills/social-media/short-drama-release-manager/"
diff -qr "$HERMES_SKILL_SOURCE" \
  "$HOME/.hermes/profiles/social/skills/social-media/short-drama-release-manager"
```

SOUL 新增第三条预授权：正文必须逐字来自已评审的 `$HERMES_SKILL_SOURCE/references/soul-preauthorization.md`，只允许固定 `shortdrama_ctl`、四张目标表、actor gate、人工字段边界和原会话通知；不继承 competitor/social-pipeline 的 writer 或 Sheet 权限。插入后提取 live SOUL 对应 block，与 source block 分别计算 SHA-256；哈希不一致则不重启 gateway。

- [ ] **Step 3: targeted restart 和健康读回**

动作时确认后只重启 `ai.hermes.gateway-social`。验证 launchctl PID 变化、gateway heartbeat、`# social-assistant` ping、Skill discovery、其他 profiles PID 不变。不能从 restart 命令 exit 0 推断消息可达。

- [ ] **Step 4: 安装新 launchd**

动作时确认后运行 `install_launchd.sh`；read-back 新 label。历史 `com.gengrowth.shortdrama-feishu-sync`若存在，先只读确认状态；单独确认后 pause/bootout，保留 plist，不删除。

- [ ] **Step 5: 真实 Social Bot E2E**

按顺序完成：

1. 授权用户查询四表并追溯发布→采集来源；
2. 授权用户预览并确认一条多字段选剧写入，readback verified；
3. 未授权用户写入被拒，Base revision 不变；
4. “同步短剧数据”立即收到 run_id；
5. “短剧同步进度”返回持久化 step/counters；
6. 终态主动回原会话；
7. partial canary 不覆盖旧指标，`manual_fields_changed_by_sync=0`。

- [ ] **Step 6: 切换 Google 人工入口并开启七天观察**

动作时确认后将旧 Google 三张人工业务页/文件权限改为只读，保留 `采集数据`公式用于观察；不改 TikTok Daily Metrics。记录权限前后读回。

- [ ] **Step 7: 观察真实北京时间 08:00 自然运行**

至少观察一次自然调度生成 schedule run_id、Collector terminal、Base readback 和 Social 终态；手动 `--force`或立即 kickstart 不算自然运行证据。

- [ ] **Step 8: 连续七天 acceptance ledger**

每天记录：

- 三张人工表 `baseline + audited creates - audited archives`；
- 采集数据 Post ID 集合与 SQLite latest；
- 每轮 `manual_fields_changed_by_sync=0`；
- duplicate SD/SR/Post ID=0；
- Job terminal/notification；
- 旧 Google 和其他 Social OS 无意外变化。

任何一天失败，修复后七天窗口从新的连续成功日重新计算，不能把非连续成功拼成七天。

- [ ] **Step 9: 完成或回滚**

七天全部通过：旧 Google 短剧业务表标记 archived/read-only，Base 成为正式入口，TikTok Daily Metrics 保留。

观察期失败：暂停新 Base writer/launchd，导出 Base 人工变更，恢复 Google 编辑权限并按审计补回；不删除 Base、SQLite、Job/Audit、旧脚本或证据。

## Spec Coverage Map

| v5.0 section | Implemented/verified by |
|---|---|
| 1. 一句话目标 | Tasks 1–13 complete product path |
| 2. 已锁定决策 | Global Constraints; Tasks 1, 8–13 |
| 3. 已验证现状 | Task 11 current diff audit; Task 12 live preflight |
| 4. 范围与非范围 | File structure; Tasks 9–11 no core/HTTP/old-flow changes |
| 5. 总体架构 | Tasks 1–10 code; Tasks 12–13 deployment |
| 6. 四表数据模型/视图 | Tasks 1, 3, 4, 7, 12 |
| 7. 字段所有权 | Tasks 1, 4, 6, 8; human hash acceptance |
| 8. Social Bot 产品入口 | Task 10 source/contract; Task 13 live E2E |
| 9. Runner 契约 | Tasks 1–9 |
| 10. 采集与回填 | Tasks 5, 8; Task 12 migration verify |
| 11. 错误与恢复 | Tasks 1–8 named errors/tests; Task 13 partial canary |
| 12. 安全与权限 | Tasks 1, 3, 6, 9, 10; Tasks 12–13 action-time gates |
| 13. 迁移与七天切换 | Tasks 7, 12, 13 |
| 14. 测试与验收 | Every task red/green checks; Tasks 11–13 full/live verification |
| 15. 优先级和实施顺序 | Task order 1–12 and dependency interfaces |
| 16. 完成定义 | Final Verification Matrix and Implementation Exit Gate |

## Final Verification Matrix

| Spec requirement | Proof |
|---|---|
| 四表正式 Base | schema metadata、table/field IDs、views、dashboard read-back |
| 采集数据是最新来源 | Post ID 集合/字段与 SQLite latest 全量对比 |
| 日历史只在 SQLite | Base 无历史快照表；SQLite snapshot ranges 保留 |
| 选剧池人工所有权 | field-owner tests、Base field protection、sync human hash |
| Social actor 权限 | authorized/unauthorized real Feishu E2E |
| async run_id | start/status/terminal message and JobStore rows |
| 每日北京时间 08:00 | natural launchd run evidence |
| 旧流程不变 | git scope、回归 tests、旧 Sheet/runner read-back |
| 七天切换 | 连续七天 acceptance ledger，无拼接日期 |
| 可回滚 | backup checksums、retained plist/Base/Google state、dry-run restore checklist |

## Implementation Exit Gate

不得声称“完成”直到：

1. Ops 与 Hermes 两仓测试、review 和精确 diff 均通过；
2. migration verify 四表逐字段通过；
3. 真实 Social Bot E2E 通过；
4. 观察到自然 08:00 运行；
5. 连续七天 acceptance ledger 全绿；
6. 旧 Google/Social OS 未改变；
7. 回滚证据可读且未执行破坏性清理。
