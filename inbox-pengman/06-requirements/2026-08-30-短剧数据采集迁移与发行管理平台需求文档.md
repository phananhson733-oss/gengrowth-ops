---
title: 短剧数据采集迁移与发行管理平台需求文档
date: 2026-08-30
version: v4.1
status: draft
owner: Pengman
type: automation-requirements
project: short-drama
updated: 2026-08-31
assignee: 王志彪（彪哥）
platform: 待定（飞书多维表格优先；不可行则 Google Sheets / 其他）
---

# 短剧数据采集迁移与发行管理平台需求文档（v4.1）

> 本文档**独立完整**，不依赖任何旧文档，彪哥只读这一份即可开工。
> 目标受众：王志彪（彪哥）。目标是把他接手过来的这套 TikTok 短剧数据采集+发行管理跑起来，并满足王玲（Lynne）的新需求。

## 核心定位（先看这个）

**「短剧发行管理」才是核心，采集器只是它的数据来源。**

- 我们要做好的工具 = **短剧发行管理**（账号台账 / 发布记录 / 选剧池 3 张业务表），让录入、查看、筛选、复盘更方便。
- 采集器（抓 TikTok 公开数据的代码）是**支撑**，负责每天自动产出播放/点赞/评论/链接/Post ID，回填到短剧发行管理里。
- 如果要迁到飞书，**只需要把「短剧发行管理」这一层迁过去**；采集器本身、以及中间层数据表（TikTok Daily Metrics）**不需要迁飞书**。

当前链路（现状）→ 目标链路：

```text
现状：
  采集器(Pengman MacBook) → SQLite → Google Sheets「TikTok Daily Metrics」→ IMPORTRANGE → Google Sheets「短剧发行管理」

目标：
  采集器(彪哥 Mac mini, 7×24) → SQLite(本地历史, 保留)
          └→ 飞书「短剧发行管理」多维表格（账号台账 / 发布记录 / 选剧池 + 自动回填数据）
```

**关键结论：迁飞书时可以跳过「TikTok Daily Metrics」这层。** 飞书不能像 Google Sheets 那样 IMPORTRANGE 引用 Google Sheets，所以正确做法是让采集器**直接写飞书表**。采集器已有写飞书的能力（`feishu_sync.mjs` 现在就在写粉丝数），只需扩展成"也写帖子指标"即可。

---

## 0. 背景与现状（一句话）

现在这套 TikTok 短剧数据采集，**纯粹跑在彭满（Pengman）的个人 MacBook 上**，有三个问题：

1. **慢、占本机资源**：采集脚本要逐条请求 TikTok 公开页 + 用 yt-dlp 取每条帖子详情，跑完一整轮会占用 Pengman 的 Mac。
2. **周末不自己跑**：脚本由 macOS launchd 定时，Mac 必须开机联网才跑；人离开/关机就不跑了。
3. **可视化差**：Lynne 想要飞书多维表格的可视化 + 与其他飞书表格联动，现在的 Google Sheets 方案不够直观。

因此需要把采集**迁移到彪哥那台 Mac mini 上 7×24 定时跑**（最高优先级），并把「短剧发行管理」落到飞书多维表格（次高优先级）。

---

## 1. 参考链接（现有数据现状，彪哥可打开看）

当前两套 Google Sheet 的实际位置：

| 表                                          | 链接                                                                                  | 说明                                                                                                                                             |
| ------------------------------------------ | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| **短剧发行管理**（业务表，**核心，要迁飞书的就是它**）            | https://docs.google.com/spreadsheets/d/1BbOcWUVrhRsnuSAs9LcyCuYWTrauPxtJWI12Esao7p0 | 3 个业务 tab：账号台账 / 发布记录 / 选剧池（+1 个「采集数据」tab 用于引用原始数据）。含手动字段（剧名/剧分类/RS收益/备注等）和自动回填字段（播放/点赞/链接/Post ID）                                            |
| **TikTok Daily Metrics**（中间层，**飞书方案下可退役**） | https://docs.google.com/spreadsheets/d/17NOiX9VGozHEgthpSbBN-2dyf4rJRsTQkmLubBwnICQ | 采集器每次运行全量重建的 5 个 tab：`accounts_latest / account_history / posts_latest / post_history / runs`。它的作用只是把采集结果投影到 Google Sheets 供短剧发行管理引用；迁飞书后可跳过这层 |

> 两张表都需有对应 Google 账号的查看权限才能打开；无权限时会让彪哥帮忙开通或导出数据。

---

## 2. 目标与优先级（要按这个顺序做）

| 优先级        | 目标                                          | 说明                                                  |
| ---------- | ------------------------------------------- | --------------------------------------------------- |
| **P0（最高）** | 采集迁到彪哥的 Mac mini，每天自动跑，**不依赖 Pengman 本机**   | 采集器是数据来源，必须稳定 7×24                                  |
| **P0**     | **保留短剧发行管理的全部字段和全部数据**（3 张业务表）              | 迁移不能丢任何已有字段/数据                                      |
| **P1（次高）** | 「短剧发行管理」落到**飞书多维表格**，可视化 + 与其他飞书表联动         | Lynne 的核心诉求；**只迁这一层业务表**；**最好能上，但非必须**（做不了走第 8 节退路） |
| **P1**     | 提供一个**手动同步入口**（如 social 机器人里说"同步短剧数据"）      | 随时手动触发采集+回填                                         |
| **P2**     | 若飞书方案受版本/权限限制做不了，退而求其次                      | Google Sheets / Notion / 其他                         |
| **P2**     | 完成 Lynne 剩余需求（剧 ID 自动生成、数据按剧/按账号聚合）；来源字段已落地 | 见第 7 节                                              |

**一句话主线：先让彪哥那边稳定每天跑采集并保住旧数据，再把「短剧发行管理」落到飞书（或退路平台），最后补 Lynne 的剩余字段。**

---

## 3. 现有采集器（彪哥要接手的那套代码）

完整的采集+同步代码在 Pengman 仓库的这个目录（可整体迁移到 Mac mini）：

```
~/gengrowth-ops/inbox-pengman/tools/tiktok-public-capture/
```

目录内容（整体迁移）：

| 文件 | 作用 |
|---|---|
| `collect_tiktok_public_data.mjs` | **主采集入口**。抓账号 + 帖子指标，落 SQLite/CSV/XLSX，再触发同步 |
| `persistence.mjs` | 写 SQLite（唯一事实源）+ 同步 Google Sheets |
| `feishu_sync.mjs` | 同步到飞书（现仅写粉丝数，飞书方案需扩展写帖子指标） |
| `run_scheduled.sh` + `launchd/` + `install_launchd.sh` | 定时任务（macOS launchd）整套 |
| `.env` / `.env.example` | 凭证配置（Google 服务账号、飞书 app、各表 ID），**被 git 忽略，需单独迁移** |

### 3.1 采集原理（纯公开，无付费 API，无代理）

- **账号清单**：脚本里 `usernames` 数组硬编码，当前 11 个账号（含全部短剧号）：
  `astrologywiki`、`dramapenelope`、`miraaastrology`、`shirley527146`、`shirley5276973`、`shirley5278789`、`dramadetour0`、`jolienqaq`、`dramaclips0364`、`dramavault163`、`dramaexpedition`
- **账号层**：请求 TikTok 公开主页 + embed 页，解析页面内嵌 JSON，取昵称/粉丝/关注/获赞/签名和视频列表。
- **帖子详情层**：逐条取播放/点赞/评论/收藏/分享，两条数据源路径，**yt-dlp 可选、不是必须**：
  - 首选（可选）：`yt-dlp --dump-single-json` 直接从 TikTok 取；**可以不装**。
  - 降级（纯 HTTP，无需额外安装）：`tikwm.com` API，能返回相同指标（代码已映射成 `view_count/like_count/comment_count/save_count/repost_count`）。
  - 再失败记 error。**需要 Node 装好**（当前 Node 路径写死为 `/Users/pengman/.cache/...`，迁移时须改成 Mac mini 实际路径）；不装 yt-dlp 就固定走 `tikwm.com`（先验证稳定性，见第 10 节）。
- 字段缺失记 `null`（不写 0），标记 `partial`。

### 3.2 本地落盘与两路同步

数据唯一事实源是本地 SQLite（`output/tiktok_metrics.sqlite`），再分两路同步且互不阻断（任一失败不影响其余）：

- **Google Sheets** → 5 个 tab：`accounts_latest / account_history / posts_latest / post_history / runs`
- **飞书** → **目前仅写「粉丝」「粉丝同步时间」两字段，短剧帖子数据需新增**（这是 P1 飞书方案要动手改的地方，代码在 `feishu_sync.mjs`）

谷歌表和飞书表都是 SQLite 的下游投影，不是独立数据源。迁到 Mac mini 时直接拷贝 SQLite 继续累积即可，历史数据不丢失。

### 3.3 现有数据量（迁移时不能丢）

- 账号快照 184 行，时间范围 2026-07-20 ~ 2026-08-31（31 个自然日）
- 帖子 122 条（去重后）、帖子快照 932 行、运行记录 88 次
- 历史账号共 14 个 unique 用户名

---

## 4. P0 详细要求：迁到彪哥 Mac mini 上每天自动跑

1. 把 `tiktok-public-capture/` 整个目录迁到 Mac mini，配好：
   - Node 运行环境（**必需**）；`yt-dlp`（**可选**，不装就让采集器固定走 `tikwm.com` 取帖子指标，见第 3.1 节）
   - `.env`（含 Google 服务账号 JSON、飞书 app 凭证、各表 ID）
   - 改掉所有写死的绝对路径（`/Users/pengman/...` → Mac mini 上的实际路径）
2. 用 `install_launchd.sh`（或等价方式）装成 launchd 定时任务，**每天自动跑**，Mac mini 保持开机联网即可 7×24 覆盖。
3. 迁移完成后，输出应与 Pengman 本机跑出的**数据口径完全一致**（同一套公开抓取）。
4. 保留全部历史数据：SQLite 可直接拷贝到 Mac mini 继续累积，不要清零重建。

---

## 5. P1：手动同步入口（social 机器人触发）

需要一个"说一句话就手动同步"的入口，例如在 social 机器人里说：
**"同步短剧数据"**

触发后立即执行一次完整采集+同步（等价于手动跑一次 `collect_tiktok_public_data.mjs`），不依赖定时点。

实现方式不限（机器人命令 → 调 Mac mini 上的采集脚本 / webhook / 远程触发），由彪哥定；关键是**能手动即时触发 + 能拿到执行结果反馈**。

> **注意**：这个"手动触发"**不要用飞书的自动化流程**。飞书「自动化」里"接收到 webhook 时"这个触发条件需要更高等级的飞书会员，之前试过做不了。所以触发动作放在 social 机器人 / 本地命令行 / 其他平台即可，不依赖飞书自动化。

---

## 6. P1：飞书多维表格方案（Lynne 核心诉求）

**目标**：把「短剧发行管理」这一层业务表落到飞书多维表格，实现：
- 可视化（表格视图/看板视图）
- 与飞书上其他表格联动
- Pengman 和 Lynne 都能直接打开查看录入

**边界（重要）**：**只迁「短剧发行管理」的 3 张业务表**（账号台账 / 发布记录 / 选剧池），不迁采集器、不迁 TikTok Daily Metrics。

**数据流**：采集器在 Mac mini 跑完后，**直接从 SQLite 写飞书表**，跳过 TikTok Daily Metrics（Google Sheets）这层中间层。具体要做的改动：

1. 在飞书建立 3 张表（账号台账 / 发布记录 / 选剧池），字段对照第 7 节。
2. 扩展 `feishu_sync.mjs`：现在只写「粉丝」「粉丝同步时间」两个字段，需扩展为也写帖子指标（视频链接/Post ID/播放/点赞/评论/收藏/转发），并按账号/帖子回填到「发布记录」。
3. 一次性迁移现有业务数据：手动字段（剧名/剧分类/RS收益/备注等）+ 已回填数据，从 Google Sheets「短剧发行管理」迁进飞书。
4. TikTok Daily Metrics 可退役（或保留作备份，不再是必经环节）；本地 SQLite 继续保留历史快照，不进飞书。

**已有飞书资产（历史试验遗留，可复用或重做）**，`.env` 里已配置：
- `FEISHU_SHORTDRAMA_APP_TOKEN`、`FEISHU_SHORTDRAMA_ACCOUNTS_TABLE_ID=tblDa5G0EpxtPmax`、`FEISHU_SHORTDRAMA_RELEASES_TABLE_ID=tblFI7d9USySCqYE`、`FEISHU_SHORTDRAMA_POOL_TABLE_ID=tblc0wZ4FFiMiGVk`
- 已有 `feishu_sync.mjs` 实现粉丝数同步（按「账号ID」字段匹配，只写「粉丝」「粉丝同步时间」）

**风险提示（需彪哥核实，飞书不一定能实现）**：
- 飞书多维表格写入需要企业自建应用，且要具备表格的编辑权限；某些能力/权限范围可能要求更高的飞书版本或管理员授权。
- 已知限制：飞书「自动化」流程里"接收到 webhook"这个触发条件需要更高等级会员，**不要依赖飞书自动化来做采集触发**（触发用 social 机器人/本地命令，见第 5 节）。
- **飞书是"最好能上"，但不是必须。** 如果核实下来飞书写入因版本/权限做不了，就直接降到第 8 节退路（继续用 Google Sheets / Notion 等），核心目标（采集迁 Mac mini 每天跑 + 短剧发行管理更好用）不受影响。

---

## 7. 短剧发行管理字段清单（P0 不可丢 + Lynne 新需求）

原「短剧发行管理」表（Google Sheets，ID `1BbOcWUVrhRsnuSAs9LcyCuYWTrauPxtJWI12Esao7p0`）有 3 个业务 tab（账号台账 / 发布记录 / 选剧池）+ 1 个「采集数据」tab（引用原始数据）。**3 个业务 tab 的字段迁移到飞书时必须全部保留。** Lynne 的 3 点需求状态已标注在下方字段清单和小结里。

**Sheet A：账号台账**
`账号名 / 主页链接 / 粉丝数 / 所属组 / 定位垂类 / 表现形式 / 状态 / 数据日期`

**Sheet B：发布记录（核心）**

| 字段 | 数据来源 |
|---|---|
| 日期 | 手动 |
| 账号名 | 下拉（引用账号台账） |
| 主页链接 | 由账号名带出 |
| 剧名 | 手动（定时发布先填） |
| 剧ID（RS Boost） | 手动（可选） |
| 剧分类 | 手动（下拉，RS Boost 分类） |
| 视频链接 | **自动回填**（帖子公开后） |
| Post ID | **自动回填** |
| 播放量 / 点赞 / 收藏 / 转发 / 评论 | **自动回填** |
| RS收益 | 手动（RS Boost 后台，每周一次） |
| 发布状态 | 下拉：已排期 / 待公开 / 已公开 / 已回填 |
| 备注 | 手动 |

**Sheet C：选剧池**（当前共 16 部剧）

| 列 | 字段 | 说明 / 来源 |
|---|---|---|
| A | 剧名 | 手动 |
| B | 剧ID | **待自动生成**（Lynne 需求 #1 ⏳，当前为空） |
| C | 剧分类 | 手动（如「狼人, 复仇」「扮猪吃虎」） |
| D | 上线日期 | 手动 |
| E | 生命周期 | 手动（在推 / 新剧…） |
| F | 是否已排期 | 手动 |
| G | 备注 | 手动 |
| H | 推荐理由 | 手动自由文本（Lynne 需求 #2「选择的原因」✅） |
| K | RS Boost 分类（待确认） | 手动 |
| M | 账号组 | 手动 |
| O | 账号状态 | 手动 |
| P | 平台 | 下拉单选：ReelShort / DramaBox / ShortMax / TopShort / 其他 |
| Q | 语言 | 下拉单选：英语 / 罗马尼亚语 / 法语 / 日语 / 俄语 / 印尼语 / 中文 / 西班牙语 / 葡萄牙语 / 其他 |
| R | 来源 | 下拉多值（逗号分隔，可手输）：Reelshort榜单 / Google Trends / 至真选剧台（Lynne 需求 #2 ✅） |
| S | 推荐人 | 下拉多值（逗号分隔，可手输）：彭满 / 高璇 / 马博洋（Pengman 补充） |

> 说明：`来源`/`推荐人`是"非严格下拉"，多值用逗号分隔（Google Sheets 原生下拉不支持多选）。「推荐理由/平台/语言/来源/推荐人」为新增字段，原有 16 部剧数据未动。
> 迁飞书后：`平台/语言`保留单选，`来源/推荐人`建议换成飞书原生多选字段。

**Lynne 需求小结（P2）**

| 需求 | 现状 |
|---|---|
| 1. 剧 ID 自动生成 | ⏳ 未落地（选剧池 B 列，当前为空，需彪哥实现） |
| 2. 「来源」字段（选择的原因） | ✅ 已落地（「来源」+「推荐理由」两字段） |
| 3. 数据采集关联到剧，按剧/账号看数据 | ⏳ 未落地（需把回填数据关联到具体剧） |

### 数据回填机制（原有，需保留）

回填逻辑（与平台无关，飞书/Google Sheets 都适用）：

- **定时发布**：先填剧名/剧分类（状态=已排期）占位 → 帖子公开后补链接或自动匹配 → 数据自动回填（状态=已回填）。
- **匹配桥接**（按可靠性）：**用户补视频链接（最高）> 精确 post_id > 账号+发布时间窗（辅助）**。
- **现状（Google Sheets）**：通过 `IMPORTRANGE + VLOOKUP` 从「TikTok Daily Metrics」自动带出播放/点赞/收藏/转发/链接/Post ID、账号台账粉丝数。
- **迁飞书后**：由采集器（扩展后的 `feishu_sync.mjs`）直写飞书表，回填到「发布记录」和「账号台账」（见第 6 节）。

---

## 8. 退路方案（若飞书做不了）

退路优先级：**Google Sheets（现有方案，零改动，继续用）→ Notion → 其他能可视化+联动的形式**。

- Google Sheets：维持现状（采集器写 TikTok Daily Metrics + 短剧表公式引用），剩余需求（剧 ID 自动生成、按剧/按账号聚合）用公式或辅助表实现。
- Notion 或其他平台：只搬「短剧发行管理」的 3 张业务表，采集器照常跑（Mac mini + SQLite），是否由采集器直写该平台由彪哥评估。

无论走哪条退路，**采集器迁到 Mac mini 这一层都是必做且不变的**。

---

## 9. 交付验收标准

1. **P0**：采集在彪哥 Mac mini 上每天自动跑，Pengman 本机关机/周末也照跑，数据口径与本机一致。
2. **P0**：迁移后账号台账/发布记录/选剧池的**全部字段、历史数据完整**，无丢失。
3. **P1**：social 机器人说「同步短剧数据」能立即手动触发一次同步。
4. **P1（可选）**：「短剧发行管理」落到飞书多维表格，可视化可看、可与其他飞书表联动。**若飞书因版本/权限做不了，改走第 8 节退路（Google Sheets / Notion 等）并说明原因，本项不算失败。**
5. **P2**：选剧池剧 ID 能自动生成；采集数据能关联到剧，支持按剧/按账号看数据（「推荐理由/平台/语言/来源/推荐人」字段已落地，验收时确认迁移后仍完整）。

---

## 10. 待彪哥确认 / 需要 Pengman 提供

1. Mac mini 环境：能否装 Node？`yt-dlp` 可选——不装则采集器固定走 `tikwm.com`（需先验证其对**视频**帖子的指标稳定性与频率限制）。
2. 飞书权限/版本是否具备？（决定飞书能否落地，还是走第 8 节退路。已知：飞书「自动化」webhook 触发需高级会员，不依赖它做采集触发）
3. social 机器人「同步短剧数据」命令的接入方式（webhook / 机器人跳转调脚本）？
4. `.env` 凭证、Google 服务账号 JSON、飞书各表 ID，需从 Pengman 本机密传过去。
5. 「剧 ID 自动生成」的规则（递增数字？UUID？）——B 列字段已存在但当前为空，需彪哥实现。

---

*需求 v4.1 起草于 2026-08-30，2026-08-31 更新。取代并独立于旧《2026-08-20-短剧账号发行管理平台需求文档》；彪哥只需阅读本文档。*
